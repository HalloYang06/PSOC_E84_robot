# Project Progress

## 2026-07-08

- Continued product QA for the rebuilt `app/rehab-arm-mobile-stitch` mobile App from a real phone-user path. The acceptance matrix found P0 dead ends in the bottom `社区` tab, device-page back button, profile settings button, and the debug SMS wording.
- Used Stitch MCP project `9733571660387876930` to generate a mobile `社区内测` screen, then added `apps/web/public/rehab-arm-mobile/community.html` and wired all bottom `社区` tabs to it. The page is honest about closed-beta status and routes users to `问康复师` instead of pretending a live community exists.
- Added a formal App backend康复师 Agent endpoint: `POST /api/rehab-arm/app/v1/agent/messages`. It reuses server-side model relay settings when configured, falls back to safe rule guidance when not configured, records `relay_channel=app_rehab_agent`, `client_type=app`, `purpose=rehab_agent_chat`, `scope=patient_training_guidance`, and `does_not_touch_xiaozhi_l=true`, and refuses dangerous CAN/motor/M33 override style requests. XiaoZhi/L WebSocket and semantic L chain were not touched.
- Improved the device binding page toward real Android Bluetooth use: browser mode now clearly says it needs the Android App Bluetooth bridge, and the WebView code can use the existing Capacitor `RehabArmSpp` native plugin as a compatibility bridge by mapping `listBondedDevices()` into the current device-search UI. Account binding remains separate from actual SPP connection/M33 ACK.
- Updated the internal-test phone verification copy from `测试环境验证码` to `当前为内测验证模式，内测验证码：xxxx`, added profile settings feedback, and fixed device/AI top back actions.
- Removed remaining static demo chat from `ai-plan.html`; the message screen now opens with a real Agent empty state and renders only user-sent messages plus responses from `POST /api/rehab-arm/app/v1/agent/messages`.
- Rebuilt the Android debug APK as version `1.0.11` (`versionCode=12`) and copied it to `apps/web/public/downloads/rehab-arm/lingdong-rehab-arm-debug.apk`. Final SHA256 is `070EB7FB9B51F774596C9DDAD32BC62B6FD8EB059960500F5F373D8624A167BB`, size `4,184,675 bytes`; APK asset inspection confirmed `community.html`, `device.html`, `ai-plan.html`, `home.html`, and `profile.html`, with the Agent endpoint and `RehabArmSpp` bridge code present and the old sample chat absent.
- Validation passed: `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`14 passed`), `node apps/web/scripts/qa-profile-auth-lock.cjs`, `node apps/web/scripts/qa-device-binding-isolation.cjs`, `node --check` for PWA/Android runtimes, `npm run build:debug`, local Playwright click QA for home bottom nav -> message Agent -> device SPP search/bind -> download page, and injected `RehabArmSpp` WebView fallback QA showing a paired SPP device in the device list.
- Closed the profile phone-binding backend gap that the shipped 1.0.11 page was already calling. Added `POST /api/rehab-arm/app/v1/account/phone-verifications` and `POST /api/rehab-arm/app/v1/account/phone-verifications/{verification_id}/confirm`; internal-test mode returns `debug_code`, confirmation persists `phone_number` / `phone_verified_at` on the rehab App profile, and `/me/profile` / `/me` return `profile.phone.value` plus `profile.phone.verified` after refresh.
- Validation passed after the phone-binding fix: `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`14 passed`) and Python compile checks for the changed backend modules.
- Still not certified as a real hardware training release: current M33 firmware pairing, SPP frame send, ACK/sensor evidence, M33 `m33_accepted`, and preflight-to-session start must be verified on an Android phone with the actual device.

## 2026-07-04

- Repaired and redeployed the rehab-arm control-room `3D / 孪生` page after the previous fix was reported ineffective. The IK dry-run coordinate fields now preserve focus and typed values during Stitch iframe/dashboard refresh by reusing the focused panel and keeping unsent values in an iframe draft until generate/export.
- Bridged the visible Stitch URDF upload control in `/rehab-stitch/twin.html` to the real React `Arm3DOverview` / Three.js URDF loader. Uploading from the Stitch panel now sends the selected `File` into the real loader instead of only changing the Stitch label.
- Cloud validation passed on `http://106.55.62.122:3001/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control`: Playwright typed `0.456/-0.137/0.294` character by character with focus preserved, then uploaded `codex_bridge_test.urdf`; `#codex-twin-runtime-stage` reported `已导入 codex_bridge_test.urdf` and parsed `codex_test_joint`. Evidence is under `docs/screenshots/rehab-arm-twin-urdf-bridge-qa/`.
- Validation commands: `npm --workspace apps/web run build` locally and cloud `npm run build:web` plus `RESTART=1 bash scripts/start-cloud-prod.sh`; both passed with only pre-existing hook/Tailwind/WebGL warnings.

- Integrated the old tested Android App Bluetooth protocol into the rehab-arm App backend contract. The API now exposes `m33_legacy_spp_profile` through public config/catalog and attaches `legacy_transport_frame` records to App BLE-message payloads.
- Protocol source: `D:\app\RehabRobotArm\RehabRobotArm\PROTOCOL.md` plus the old Android `BluetoothManager.kt` / `ProtocolParser.kt`. Transport is Bluetooth Classic SPP/RFCOMM, UUID `00001101-0000-1000-8000-00805F9B34FB`, UTF-8 JSON with `\n` packet delimiter.
- The backend generates old-protocol frames only for supported command shapes: `training_plan_push -> memory`, `training_session_start_request -> execute_memory`, `training_pause_request -> stop_memory`, and `training_stop_request -> stop`. Status/hello/diagnostic App messages remain backend records with `sendable=false` and `wire_text=null` so the phone bridge does not send undefined M33 commands.
- Release state changed from "hardware protocol missing" to "legacy SPP profile available, current M33 firmware confirmation and Android native Bluetooth bridge pending." Browser/PWA JavaScript still cannot open RFCOMM directly; a Capacitor/Android native bridge is required for real phone hardware communication.
- Safety boundary unchanged: session start still requires current-version `m33_accepted`; old SPP frames are compatibility transport evidence only and do not expose CAN, raw motor setpoints, M33 override, or emergency-stop release.
- Clarified and tested the shared AI relay split for XiaoZhi/L and App training planning. App public config now advertises AI draft endpoints plus `ai_relay_contract`, and generated App drafts store `relay_channel=app_training_planner`, `client_type=app`, `purpose=training_plan_draft`, `scope=rehab_training_planning`, and `does_not_touch_xiaozhi_l=true` in `context_snapshot.ai_planner`.
- The App route reuses server-side model relay provider settings only as model configuration; it does not call or modify XiaoZhi WebSocket/L transport. Draft output remains `ai_draft_only_not_execution_permission`, then must be accepted, synced to M33, accepted by M33, and pass readiness before a training session can start.
- Validation passed with `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`12 passed`). No frontend/Stitch files were changed for this backend contract task.
- Continued the App AI training-planner closure. `POST /training-reports/{report_id}/draft-next-plan` now uses the same App training-planner relay path and writes the same `context_snapshot.ai_planner` metadata as direct draft generation, while `rehab_app.ai_training_draft.generated` audit events mirror `relay_channel`, `client_type`, `purpose`, and `scope` for unified platform visibility. XiaoZhi/L WebSocket, ASR/TTS, M55 input, and semantic-routing code were not changed.
- Validation passed again with `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`12 passed`, deprecation warnings only).
- Closed the QA concerns from the App AI relay audit split. `public-config.rehab_app.ai_relay_contract` now exposes `relay_channel=app_training_planner`, and AI-generated/fallback training plans are recursively stripped of dangerous control-shaped keys before persistence or accept-to-plan. Added regression coverage for dangerous keys inside nested `emg_policy` and `safety_constraints`.
- Validation passed with `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`13 passed`, deprecation warnings only).
- Used Stitch MCP to regenerate the mobile `AI 规划` page as `AI 规划 - 康复智能体 (中文版)` and replaced the App `ai-plan.html` with the Stitch output plus minimal compatibility edits: Chinese clinical copy, no fake model/connection values, preserved `data-arm-ai-*` and `data-role="ai-draft-*"` hooks, loaded `mobile-bridge.js`, and kept the AI draft -> accept -> plan -> M33 -> preflight safety chain visible.
- Validated the AI page locally at 390 px mobile viewport. Evidence: `docs/screenshots/rehab-arm-ai-agent-stitch-qa/ai-agent-mobile-390-v3.png` and `qa-metrics-v3.json`; no horizontal overflow, all AI binding hooks present, safety copy includes M33/CAN/急停 boundaries, and minimum button height is 48 px. `node --check` passed for PWA and Android `mobile-bridge.js`; `npm run sync:web` copied the Stitch page into the Android wrapper.
- Cloud API call check with the test account successfully created an AI draft, but the cloud response did not include `context_snapshot.ai_planner`, showing the cloud backend had not yet deployed the latest App AI relay metadata commit. The cloud draft therefore must be treated as a callable rule/fallback draft, not proof of an external model call.
- Deployed the App AI agent flow to cloud at `build_sha=7d41dafc` on `app/rehab-arm-mobile-stitch`. Cloud `/api/health` and `/api/rehab-arm/app/v1/public-config` now expose the deployed build and `ai_relay_contract`; the App planner uses server-side Qwen `qwen-plus` config with `status=external_used` while keeping `relay_channel=app_training_planner`, `client_type=app`, `purpose=training_plan_draft`, `scope=rehab_training_planning`, and `does_not_touch_xiaozhi_l=true`.
- Ran phone-user QA on `http://106.55.62.122:3001/rehab-arm-mobile/ai-plan.html` at 390 px. The user can log in, generate a model-backed draft, see `模型已调用：qwen-plus`, accept it as a normal training plan, and then see the same accepted plan with the required next step: sync to device, wait for `M33 accepted`, then pass preflight. Evidence: `docs/screenshots/rehab-arm-cloud-ai-agent-user-qa/14-ai-agent-before-login-mobile.png` through `17-ai-agent-accepted-mobile.png`, plus `qa-result-final-ok.json`.
- Fixed two user-view AI planner issues discovered during cloud QA: external model prompts now serialize report context safely before calling the relay, and the mobile AI page disables accepting stale drafts while regenerating and prioritizes the just-accepted plan over older open drafts returned by `/me`.
- Repaired the mobile `我的 / 训练活动` calendar closure after user review. The Stitch profile page no longer ships a fake `Oct 2023` heatmap, fake `4 本周训练次数`, or inert `查看日志`; it now exposes data hooks and `mobile-bridge.js` renders the current month, 14-day activity grid, weekly count, and recent log entries from `/me.care_timeline`.
- The profile page now treats empty state as a real product state: when no persisted training sessions exist, it says `暂无本周训练记录`, shows a real next step (`去创建计划`), and routes to `training-library.html`. When timeline records exist, recent entries route to the relevant App page (`ai-plan.html`, `report.html`, `device.html`, or `training-library.html`) instead of pretending a local log exists.
- Cleaned additional profile/demo wording at bridge time so first use no longer claims `System OK`, `患者 A`, bound M33, active EMG, enabled voice, or family/therapist permissions without backend evidence. The AI page copy was also softened from engineering terms (`M33 权限`, `DRAFT`, `EMG`) into user-facing wording while keeping the safety boundary.
- User-view QA evidence: local 390 px run at `http://127.0.0.1:8798/profile.html` saved `docs/screenshots/rehab-arm-profile-calendar-qa/profile-mobile-local.png` and `profile-mobile-local-result.json`; the result confirms `hasOct2023=false`, `hasFakeWeekly=false`, `hasSystemOk=false`, `hasPatientA=false`, `weekly=暂无本周训练记录`, and `logTarget=training-library.html`. Cloud 390 px run before deployment saved `profile-mobile-cloud-login.png` and confirms the deployed page is still stale, so cloud deployment is required before the user will see this fix.
- Validation passed: `node --check apps/web/public/rehab-arm-mobile/mobile-bridge.js`, `node --check apps/mobile/rehab-arm-android/www/mobile-bridge.js`, and `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`13 passed`, deprecation warnings only).
- Continued phone user-view QA across the deployed mobile pages. The scan found `report.html`, `emg.html`, and `training-session.html` still showed demo-like values (`100%`, `15m`, fake score, EMG microvolts, `98%` confidence, `TRN-8842-X`, `8/12`, `30%`, and active M33 execution copy). These pages now default to backend-evidence waiting states unless real report/EMG/session data exists.
- Local repair QA passed for `report.html`, `emg.html`, and `training-session.html` at 390 px with no flagged fake-real-time terms and no horizontal overflow. Evidence: `docs/screenshots/rehab-arm-mobile-user-qa-20260704-round2-local-fix3/qa-results.json`.
- Extended the App QA wordlist after manually reading rendered `bodyStart` output. The mobile bridge now also removes misleading static values from home/training-library/device/report first paint, including `20 mins`, `45%`, `M33 已接收`, `昨天`, fake therapist attribution, `增加 5%`, `120ms`, `45ms`, `85%`, and `安全门控 (Gatekeeper)`. Offline/unauthenticated first paint now also replaces `M33 ACTIVE`, `允许安全训练`, `急停已就绪`, and `2小时前` with waiting states.
- Local expanded QA passed for `home.html`, `training-library.html`, `device.html`, and `report.html` at 390 px with the expanded fake-data term list and no horizontal overflow. Evidence: `docs/screenshots/rehab-arm-mobile-user-qa-20260704-round3-local-v2/qa-results.json`.
- Cloud expanded QA passed on the deployed mobile pages before this interaction fix: `home.html`, `training-library.html`, `ai-plan.html`, `device.html`, `emg.html`, `profile.html`, `report.html`, `training-session.html`, and `bluetooth-debug.html` had `badTerms=[]`, `horizontalOverflow=false`, and no console errors. Evidence: `docs/screenshots/rehab-arm-mobile-user-qa-20260704-round3-cloud-final/qa-results.json`.
- Continued user-view interaction QA and fixed a real button dead end. `insertWorkflowPanel()` had an inverted guard, so logged-in users could not see the backend workflow panel; `查看下一步`/`查看流程` could be silent; `刷新报告` did not explain empty report state; and `新建计划 (AI 生成)` needed to route directly into the AI planner. The PWA and Android `mobile-bridge.js` now show the backend workflow panel, route AI planning correctly, refresh reports with an empty-state toast, and keep sync/stop buttons evidence-only.
- Validation passed after the interaction fix: `node --check apps/web/public/rehab-arm-mobile/mobile-bridge.js`, `node --check apps/mobile/rehab-arm-android/www/mobile-bridge.js`, and `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`13 passed`, deprecation warnings only). Local static interaction QA evidence is under `docs/screenshots/rehab-arm-mobile-user-qa-20260704-interaction-local/`; that run also confirmed local `127.0.0.1` static pages cannot directly call the cloud API because of CORS, so final end-to-end UX QA must use the cloud same-origin mobile URL.
- Deployed the interaction fix to the actual production runtime root and verified the cloud mobile user path after clearing service-worker/cache state. `mobile-bridge.js` is now served from `http://106.55.62.122:3001/rehab-arm-mobile/mobile-bridge.js` with the latest workflow CTA and status wording logic; cloud health reports `build_sha=16f919fe`.
- Final 390 px cloud user QA passed for `查看下一步`, `新建计划 (AI 生成)`, `生成 AI 草稿`, `刷新报告`, `查看流程`, and `记录停止请求`: no console errors, no `待绑定后端` false status, workflow panel visible, and every tested button either navigates or shows a clear toast. Evidence: `docs/screenshots/rehab-arm-mobile-user-qa-20260704-interaction-cloud-live-final/interaction-results.json`. The AI planner currently returns a safe rule draft with `model_relay_config_incomplete`, so production model relay configuration still needs follow-up before claiming external-model UX.
- Fixed the production App AI planner configuration after QA showed `model_relay_config_incomplete`. The actual runtime root `/home/ubuntu/apps/ai-collab/.env` was missing `REHAB_ARM_MODEL_RELAY_API_KEY` even though the deploy worktree had it; after adding the key to the runtime `.env` and restarting API/Web, `POST /api/rehab-arm/app/v1/ai-training-drafts/generate` returned `planner.status=external_used`, `provider=qwen`, and `model=qwen-plus`.
- Cloud AI planner QA passed: direct API smoke created draft `e40f911e-cb77-4512-9bc1-83e47286f467` with `relay_channel=app_training_planner`, `client_type=app`, `purpose=training_plan_draft`, `scope=rehab_training_planning`, `does_not_touch_xiaozhi_l=true`, and no dangerous control keys. Mobile 390 px QA on `ai-plan.html` showed `模型已调用：qwen-plus`, no `model_relay_config_incomplete`, no console errors, and no dangerous CAN/current/torque/override/急停 buttons. Evidence: `docs/screenshots/rehab-arm-mobile-ai-relay-cloud-qa-20260704/`.
- Closed the next profile calendar gap from user-view QA. `/me.care_timeline` now includes persisted `training_plan` events in addition to sessions, reports, AI drafts, and offline evidence, so accepting an AI draft into a normal plan immediately creates calendar/history evidence instead of leaving the day visually empty until a session finishes.
- Updated the Stitch-derived mobile bridge for both PWA and Android wrapper: the `我的 / 训练活动` grid now highlights any persisted rehab activity, shows compact type badges (`计/训/报/AI/离`), keeps the weekly training-session count separate, and lets a date cell route to the relevant App page. This remains data binding only; the Stitch profile layout was not replaced.
- Validation passed locally and on cloud: `node --check apps/web/public/rehab-arm-mobile/mobile-bridge.js`, `node --check apps/mobile/rehab-arm-android/www/mobile-bridge.js`, and `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`13 passed`, deprecation warnings only). Cloud user-view QA on `profile.html` at 390 px confirmed `2026年7月`, `12 条本周康复活动，其中 0 次训练记录`, `training_plan` log entries, no console errors, and no horizontal overflow; evidence is `docs/screenshots/rehab-arm-profile-calendar-qa/profile-mobile-cloud-calendar-activity.png` and `profile-mobile-cloud-calendar-activity-result.json`.
- Continued cloud click-through QA from a phone user's perspective across `home`, `training-library`, `ai-plan`, `device`, `emg`, `profile`, `report`, `training-session`, and `bluetooth-debug`. Found and fixed three UX dead ends/misleading states: profile `配对新设备` now routes to Bluetooth debug, profile `云端同步` refreshes backend data instead of trying to sync a training plan, and device `一键同步训练计划` explains the exact missing trusted M33/PSoC device.
- Hardened mobile bridge text replacement so backend status/toast/workflow panels are no longer accidentally rewritten by static demo cleanup. Removed old `RoboRehab` branding at hydration time without replacing the `ArmControl` brand as if it were a missing M33 device. Cloud QA confirms device page status is `已连接后端，待绑定设备｜下一步：审核 AI 训练草稿`, no old brand remains, and no horizontal overflow or console errors appear. Evidence: `docs/screenshots/rehab-arm-mobile-click-qa-20260704-r4-status-fix/result.json` and `docs/screenshots/rehab-arm-mobile-click-qa-20260704-r6-final-check/result.json`.

## 2026-06-17

- Added a perception-only stereo RGB + pretrained YOLO framework for rehab-arm VLA intake.
- Added `POST /api/rehab-arm/v1/devices/{device_id}/vision/stereo-context`.
- The platform now stores `stereo_vision_context` as latest device telemetry and uses it when building `vla_vision_context`.
- Added backend coverage for stereo context upload, dashboard visibility, and model relay preference.
- Validated with `python -m pytest tests/test_rehab_arm_sync.py -q` in `apps/api`.
- Reworked the rehab-arm command center upper-limb/EMG section for presentation use: it now reserves an open-source GLB upper-limb muscle model slot, overlays EMG/fatigue channels, and exposes action prediction cards from `motion_prediction` / `action_prediction` / `model_outputs`.
- Added user-view QA screenshots for the authenticated local path `/projects/proj_rehab_arm/rehab-arm-control` under `docs/screenshots/rehab-arm-muscle-prediction-qa/`.
- Validated the frontend change with `npx --workspace apps/web tsc --noEmit` and browser screenshots at desktop and mobile sizes.

## 2026-06-27

- Extended rehab-arm VLA vision context so model relay receives a compact `visual_lock_summary` and `pixel_servo_hint` from the latest `stereo_vision_context`; this remains perception-only and not motion permission.
- Added a rehab-arm command-center visual lock meter inside the V evidence card: same-label frame progress, stereo-match progress, jitter, and dry-run gate state.
- Validated backend contract with `python -m pytest apps/api/tests/test_rehab_arm_sync.py -k stereo_vision_context_prefers_yolo_pair -q`.
- Validated frontend compile with `npm --workspace apps/web run build`.
- User-view screenshot QA was attempted against local web with cloud API, but local SSR authentication redirected to `/login`; no successful visual screenshot was claimed for this slice.
- Re-tested cloud login with the corrected account password and confirmed the cloud rehab-arm page opens with real stereo evidence; production web had not yet picked up commit `15b1d729`, so the new visual-lock meter was still unverified on the deployed page.
- Deployed commit `a4d1c15` to the cloud host using `.codex-cloud-ssh/tencent_lighthouse_ed25519` as `ubuntu`; `npm run build:web` passed with pre-existing hook warnings in `project-playable-shell.tsx`, and `scripts/start-cloud-prod.sh` restarted API/Web successfully.
- Captured cloud QA screenshot `docs/screenshots/rehab-arm-visual-lock-meter-qa/cloud-desktop-1600-meter-deployed.png`: V evidence, real left/right frames, and the new multi-frame visual lock meter are visible without card overlap.

## 2026-07-01

- Added a rehab-arm control-page visual memory layer: target and end-effector observations are inferred from the current stereo payload plus recent `stereo_vision_context` events.
- The A dry-run gate can now use a short visual memory pair when the real target and end effector cannot appear in the same frame; the memory TTL is intentionally limited to 5 seconds and is display/dry-run evidence only.
- Added a multi-loop health matrix for fetch-object, training, EMG assist, MuJoCo shadow, and M33 safety loops so the platform advances more than one closed loop without mixing their authorities.
- Validated with `npm --workspace apps/web run build`; then deployed the two rehab-arm control frontend files and docs to the cloud host and ran `npm run build:web` plus `RESTART=1 scripts/start-cloud-prod.sh`.
- Cloud QA evidence: `docs/screenshots/rehab-arm-visual-memory-loop-qa/cloud-desktop-1600-raw.png` and text dump confirm auto refresh, visual memory, fetch/training/EMG/shadow/safety loop rows, and dry-run boundary text on the real page.
- Added a non-vision progress panel so the demo and engineering plan do not block on object recognition: training plan, EMG assist, MuJoCo review, and data collection are shown as independent next loops while V remains hold/observe.
- Extended the non-vision path with explicit training and EMG-assist process tracks. The local L fallback now recognizes assistive phrases such as "没力/助力/肌电/帮我抬" and routes them to `assistive_emg` for display-only planning.
- Validated again with `npm --workspace apps/web run build`.
- Boundary: this does not change XiaoZhi L transport, NanoPi vision upload, MuJoCo upload, CAN, M33/M55 firmware, or real motion authority.
- Upgraded the rehab-arm human muscle section from a placeholder slot to a real Three.js GLB viewer for the open upper-limb muscle assets. The view now overlays four EMG anchor callouts on the loaded GLB and keeps the action-prediction confidence strip ready for `motion_prediction` / `action_prediction` data.
- Changed the GLB loader to tolerate partial model-load success and added camera fitting from the loaded model bounds, so upper-arm/forearm/full-arm assets can be swapped without hand-tuning the view.
- Deployed the rehab-arm frontend file plus the missing Draco decoder static assets to the cloud host; `npm run build:web` and `RESTART=1 scripts/start-cloud-prod.sh` passed, with only pre-existing `project-playable-shell.tsx` hook warnings.
- Cloud validation passed: `scripts/check_web_api_alignment.py` reported Web/API alignment for project `e201f41c-25a6-46e1-baf8-be6dcb83284c`, Draco decoder URLs return 200, and screenshot `docs/screenshots/rehab-arm-visual-memory-loop-qa/cloud-human-muscle-glb-1600-camera-fit.png` shows the real upper-limb muscle GLB with EMG anchors.
- Reworked the human muscle labels from fixed percentage stickers into Three.js projected labels: each EMG label now has a 3D anchor, is projected through the active camera every animation frame, follows model rotation/orbit controls, and is clamped into the viewport when the corresponding muscle point is near the edge.
- Cloud QA for projected labels passed with screenshot `docs/screenshots/rehab-arm-visual-memory-loop-qa/cloud-human-muscle-glb-follow-labels-clamped.png`; remaining polish is label collision/edge avoidance, not the projection mechanism.

## 2026-07-02

- Created and switched to branch `app/rehab-arm-mobile-stitch` for the rehab-arm mobile APP/backend collaboration slice.
- Connected to Stitch MCP and confirmed the mobile APP frontend project `projects/9733571660387876930`, title `灵动康复 ArmControl`.
- Reviewed the Stitch screen set: home control center, device connection/status, training library, AI plan generation, training execution, EMG monitoring, training report, and rehab profile.
- Backend direction: add the APP backend as a separate mobile-facing contract, preferably under a rehab-arm app namespace, while preserving the existing `/api/rehab-arm/v1` robot/platform telemetry and model-relay boundary.
- App backend boundary remains: user profiles, M33 BLE device binding, training-plan library/versioning/sync state, training sessions, EMG/M55 summaries, AI plan drafts, offline queues, audit logs, and platform sync evidence only. It must not expose CAN frames, raw motor setpoints, torque/current, raw position/velocity, M33 overrides, or emergency-stop release commands.
- `docs/AI_PROJECT_STRUCTURE_GITHUB.md` was requested but is not present in this repository. Current usable architecture context is `docs/medical-rehab-arm-platform-development-plan.md`, `docs/platform-agent-operating-architecture.md`, `docs/rehab-arm-nanopi-vla-mujoco-integration.md`, and `docs/rehab-arm-app-backend-prompt.md`.
- Implemented the first backend API slice for the phone APP under `/api/rehab-arm/app/v1`: profile upsert/read, M33 BLE device binding/listing, training plan create/list, sync-to-device request, training session start/finish, EMG summary, latest EMG, and M55 intent summary. Every action-adjacent response includes a `control_boundary` and remains evidence/plan-only.
- Added SQLite models and startup self-healing table creation for APP profiles, device bindings, training plans, plan syncs, training sessions, EMG summaries, and intent summaries.
- Added `apps/api/tests/test_rehab_arm_app_backend.py` covering the mobile profile/device/plan/sync flow plus training session/EMG/intent summary flow. Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q`.
- Created the first phone-runnable PWA shell from Stitch mobile screens at `apps/web/public/rehab-arm-mobile/index.html`, with `manifest.json`, `sw.js`, and eight Chinese Stitch pages. Local mobile QA at `http://127.0.0.1:4177/rehab-arm-mobile/index.html` produced screenshots under `docs/screenshots/rehab-arm-mobile-pwa-qa/` and found no 390px horizontal overflow.
- Clarified that the APP itself is not Python: Python/FastAPI is the backend layer; the phone target is currently mobile Web/PWA and can later be wrapped into an Android APK.
- Reworked the rehab-arm command center into a presentation control-room layout: VLA remains the primary top chain, while camera evidence, URDF/digital-twin view, and upper-limb muscle/EMG view are enlarged as the three main visual stages.
- Added a fixed top jump nav for `VLA 总线`, `视觉画面`, `URDF 模型`, and `肌肉肌电`; secondary mode, loop-health, and non-vision progress information is preserved in a collapsible audit drawer instead of being removed.
- Tightened the muscle label contract: Three.js-projected labels now distinguish GLB mesh-bound anchors from fallback/unbound anchors, and unbound labels are dimmed as `待绑定模型点` instead of pretending to be anatomically precise.
- Validated with `npm --workspace apps/web run build`; deployed the two rehab-arm control frontend files to the cloud host and ran `npm run build:web && RESTART=1 scripts/start-cloud-prod.sh`.
- Cloud QA screenshots: `docs/screenshots/rehab-arm-control-room-stage-qa/cloud-final-top-1600.png` shows the VLA/vision-first control-room stage, and `docs/screenshots/rehab-arm-control-room-stage-qa/cloud-final-muscle-wait30-1600.png` shows the GLB muscle model with unbound labels safely dimmed.
- Boundary: this was a frontend presentation and evidence-hierarchy pass only. It did not change XiaoZhi L transport, NanoPi uploads, MuJoCo semantics, CAN, M33/M55 firmware, or real motion authority.
- Follow-up repair after user review: the first stage pass hid too much functionality and left poor whitespace. Reworked the layout using external dashboard patterns (fixed nav, normal document flow, two-column evidence area, default-open audit drawer), reduced forced viewport heights, and kept VLA/A-gate/vision evidence visible together. Cloud repair QA screenshot: `docs/screenshots/rehab-arm-control-room-stage-qa/cloud-repair-final-1600.png`.
- After the user requested a real redesign instead of incremental patching, created a no-production-code static wireframe for the next control-room direction at `docs/qa/rehab-arm-control-room-wireframe.html`.
- The wireframe applies external dashboard/interface design references and preserves the intended page hierarchy: fixed top command bar, device rail, status ribbon, mission strip, VLA process lane, camera evidence stage, decision inspector, URDF/muscle stages, and engineering detail tabs.
- Wireframe QA screenshots: `docs/screenshots/rehab-arm-control-room-wireframe/desktop-1600.png` and `docs/screenshots/rehab-arm-control-room-wireframe/mobile-390.png`; both were checked for no horizontal overflow.
- Refined the static wireframe visual direction to avoid a purely rectangular, single-color backend look: added asymmetric panel shapes, a VLA process rail, subtle technical image-stage overlays, distinct safety/status colors, and softer industrial depth. Updated screenshots: `docs/screenshots/rehab-arm-control-room-wireframe/desktop-1600-v2.png` and `docs/screenshots/rehab-arm-control-room-wireframe/mobile-390-v2.png`.
- Deployed the production-safe interactive static preview to the cloud at `http://106.55.62.122:3001/previews/rehab-arm-control-room/index.html`; the existing production rehab-arm route was not changed.
- Preview interactions include clickable VLA lane nodes, engineering detail tabs, camera-stage hover/scan motion, and a motion toggle button. This is a layout/aesthetic preview and uses screenshot placeholders rather than live production detection data.
- Cloud QA screenshots: `docs/screenshots/rehab-arm-control-room-wireframe/cloud-preview-1600.png` and `docs/screenshots/rehab-arm-control-room-wireframe/cloud-preview-mobile-390.png`; both were captured from the public preview path after `npm run build:web && RESTART=1 scripts/start-cloud-prod.sh`.
- After user approval to replace the production look while preserving functions, applied the static-preview visual language to the live rehab-arm route as a CSS-only skin in `apps/web/app/projects/[id]/rehab-arm-control/rehab-arm-control.module.css`; the React data/function layer was not replaced.
- Backup before production styling: `docs/backups/20260702-static-preview-to-production/`.
- Validated local build with `npm --workspace apps/web run build`, deployed the CSS to the cloud, and restarted with `npm run build:web && RESTART=1 scripts/start-cloud-prod.sh`.
- Production QA screenshots: `docs/screenshots/rehab-arm-control-room-wireframe/cloud-production-replacement-1600-v2.png` and `docs/screenshots/rehab-arm-control-room-wireframe/cloud-production-replacement-mobile-390-v2.png`. Programmatic check reported no desktop horizontal overflow and confirmed key live function text remained: VLA, vision, URDF, muscle, system audit, model relay, device token, motor table, and camera keyframe.
- Fixed production deep-link behavior for the scrollable rehab-arm workspace: URLs such as `/rehab-arm-control#muscle-stage` now scroll the inner workspace to the requested stage after hydration. Verified against the user's exact cloud URL with screenshot `docs/screenshots/rehab-arm-control-room-wireframe/cloud-muscle-anchor-playwright-hash-fixed.png`.
- Added a dedicated long-page QA script `scripts/capture-rehab-arm-long-page.mjs` because the rehab-arm page scrolls inside `.workspace`, so normal viewport screenshots hide lower-page regressions. The script captures `workspace-element-full.png`, segmented viewport screenshots, and `metrics.json`. Current cloud long-page evidence is under `docs/screenshots/rehab-arm-control-room-wireframe/long-page/current/` and shows no horizontal overflow, but also confirms the redesign must continue below the first viewport.
- Stabilized the production rehab-arm route against the approved static preview `docs/screenshots/rehab-arm-control-room-wireframe/cloud-preview-1600.png`: legacy duplicate VLA/topology strips are hidden, `系统审计与模式资源` is collapsed by default, URDF and muscle stages stay side-by-side, and older engineering tools remain available below as compact panels instead of covering the main visual stages.
- Deployed the final stable layout to `http://106.55.62.122:3001/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control`; cloud `npm run build:web && RESTART=1 scripts/start-cloud-prod.sh` passed with only pre-existing `project-playable-shell.tsx` hook warnings.
- Long-page cloud QA evidence: `docs/screenshots/rehab-arm-control-room-wireframe/long-page/final-cloud-stable-layout/`. `metrics.json` reports `hasHorizontalOverflow: false`, `workspaceScrollWidth: 1322`, and `workspaceClientWidth: 1322`; `workspace-element-full.png` confirms the live top control-room structure, VLA/vision stage, URDF/muscle stage, and compact preserved tools are present.
- Polished the URDF and muscle main-stage cards with layered equipment-stage surfaces, stronger canvas framing, clearer status chips, and compact instrument-style metric tiles. Deployed CSS-only to cloud and verified with long-page QA under `docs/screenshots/rehab-arm-control-room-wireframe/long-page/urdf-muscle-polish/`; `metrics.json` still reports no horizontal overflow.
- Coordinated with Stitch MCP for the next muscle/EMG page direction. Current candidate is `projects/1207434422396149303/screens/7c36db56e9004f509fea6fbcc5eaf9e8` (`上肢肌肉数字孪生控制台 - 旗舰版 V5.0`), with QA screenshot `docs/screenshots/stitch-muscle-review/muscle-v5.png`.
- Added `docs/rehab-arm-muscle-emg-frontend-contract.md` to define the frontend view model for `emg.channels`, M55 intent, M33 safety state, BLE training-library state, assistive hint, muscle highlight mapping, and the `muscle-3d-stage` GLB migration path. This is display/review evidence only and does not change real motion authority.
- Added the missing repo-level architecture index `docs/AI_PROJECT_STRUCTURE_GITHUB.md` for the rehab-arm mobile App slice, documenting the Stitch project, PWA path, FastAPI backend namespace, file ownership, and M33 safety boundary.
- Added `apps/web/public/rehab-arm-mobile/mobile-bridge.js` and injected it into the eight Stitch mobile pages. The bridge preserves Stitch visuals while adding iframe/bottom-nav routing, local PWA fallback state, backend API refresh hooks, safe action handling, and explicit plan-sync/M33 boundary messaging.
- Updated the PWA shell and service worker so inner Stitch navigation updates the outer iframe and `mobile-bridge.js` is cached.
- Updated `docs/USER_MANUAL.md` with the phone PWA preview command, URL, optional API base configuration, and safety note.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q`; static scan confirmed all eight Stitch pages include `mobile-bridge.js`; Playwright mobile QA at `390x844` confirmed the bridge status strip renders, bottom navigation reaches `training-library.html`, no horizontal overflow appears on home/training pages, and no browser console errors were emitted.
- Improved the phone PWA install surface by adding PNG app icons (`192x192`, `512x512`, maskable `512x512`, and iOS `apple-touch-icon`), wiring them into `manifest.json`, `index.html`, and the service worker cache.
- Validation passed: browser manifest check confirmed `display=standalone`, four manifest icons, and cached `icon-maskable-512.png`.
- Deployed the phone PWA static files to the cloud route `http://106.55.62.122:3001/rehab-arm-mobile/index.html` and restarted the cloud web/API services so the new public files are served.
- Cloud validation passed: `index.html`, `manifest.json`, and `icon-192.png` returned HTTP 200; Playwright mobile QA at `390x844` confirmed the cloud page loads the Stitch iframe, has `display=standalone`, includes four manifest icons, has no horizontal overflow, and emits no browser console errors.
- Configured the Windows Android build environment for the upcoming APK wrapper: Temurin JDK 21 at `D:\Java\jdk-21`, Android SDK command-line tools at `D:\Android\Sdk`, user-level `JAVA_HOME`, `ANDROID_HOME`, `ANDROID_SDK_ROOT`, and PATH entries for Java, `sdkmanager`, `adb`, emulator, and build-tools.
- Installed and validated Android SDK packages `platform-tools 37.0.0`, `build-tools 35.0.0`, `platforms;android-35`, and `emulator 36.6.11`; added `scripts/use-android-build-env.ps1` so future shells can load the same environment.
- Added a Capacitor Android wrapper at `apps/mobile/rehab-arm-android`, with scripts to sync the Stitch-first PWA into `www`, sync Capacitor, and build a debug APK.
- Built and verified the first Android debug APK: package `com.lingdong.rehabarm`, label `灵动康复 ArmControl`, version `1.0`, min SDK 24, target SDK 36, v2 signature verified, size `4,216,514` bytes, SHA256 `C80F78CE4CCF315368ADC13C178E40CA620B2CD3A7CF48EC751CF42F72CB84ED`.
- Uploaded the debug APK to `http://106.55.62.122:3001/downloads/rehab-arm/lingdong-rehab-arm-debug.apk`; cloud validation returned HTTP 200 with `Content-Type: application/vnd.android.package-archive` and byte-range download support.
- Converted the mobile App backend from demo-like records toward a real closed-loop safety state machine. Added `/api/rehab-arm/app/v1/me`, device status, M33 sync decision update, training plan detail/edit/archive, session list/detail/progress, EMG history, AI training draft generate/read/accept, and platform sync status endpoints.
- Added `RehabAppAiTrainingDraft` plus `plan_version` on training-plan sync records. Training plan edits now increment `version`; starting a training session requires the selected device to have `m33_accepted` for the current plan version. Missing, rejected, pending, or stale-version M33 state returns `M33_ACCEPTANCE_REQUIRED` and no session is created.
- Preserved the App safety boundary: the backend still stores profiles, bindings, plans, M33 decisions, sessions, EMG/M55 summaries, AI drafts, and platform evidence only. It still does not accept CAN frames, raw motor setpoints, torque/current, raw joint position/velocity, M33 overrides, or emergency-stop release commands.
- Added SQLite self-healing for the AI draft table and `rehab_app_training_plan_syncs.plan_version` so existing local deployments can start after this schema extension.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`3 passed`). This slice intentionally did not modify Stitch/mobile frontend files; frontend adjustments should continue through Stitch MCP.

## 2026-07-03

- Patched the live NanoPi VLA vision runtime on `pi@192.168.3.36` without changing kernel, CAN, M33, or XiaoZhi/L links. `/home/pi/rehab_vla/nanopi_vla_ort_infer.py` now treats YOLOv8 end-effector confidence as raw probability by default instead of applying an extra sigmoid, and scales model-space `416x416` center boxes back into the platform `640x480` frame coordinates.
- Updated `/home/pi/rehab_vla/nanopi_vla_cpp_upload_loop.py` to pass `REHAB_END_EFFECTOR_CONF`, frame size, and input size into the ONNX runtime helper. The live cloud payload for `nanopi-m5` now reports honest waiting state when the target/end-effector are out of view: `detections.left=[]`, `end_effector_object=null`, `visual_lock_stability.stable=false`, and `pixel_servo_hint.next_step=hold_observe`.
- Evidence pulled from NanoPi after the fix is under `docs/screenshots/rehab-arm-live-end-effector-qa/` (`nanopi-after-scale-fix-left.jpg`, `nanopi-after-scale-fix-right.jpg`, `after-scale-fix-platform-context.json`, `after-scale-fix-ort-context.json`). Current scene does not contain a stable target/end-effector pair, so the correct result is no lock, not a fake box.
- Repaired the optional end-effector detector path for the NanoPi read-only stereo collector. `scripts/nanopi-rehab-arm-collect-and-upload.py` now forwards `--end-effector-onnx`, confidence, NMS, and image-size options into `nanopi-rehab-arm-readonly-agent.py`, so captured stereo keyframes can produce real `end_effector` / `gripper_tip` OpenCV DNN evidence instead of remaining in detector-waiting state.
- Offline QA with the previously labeled/trained dataset at `D:\vla_dataset\20260627_213112` confirmed the trained `best.onnx` detects the sample end effector through the full collector dry-run path: `end_effector` confidence `0.811` and `gripper_tip` confidence `0.7966` on `mono_000035.jpg`. The earlier offline contact-sheet comparison remains under `docs/screenshots/rehab-arm-end-effector-offline-qa/`; mean best IoU was about `0.635`, so live use should treat boxes as evidence that still needs temporal stability and later retraining, not a final grasp pose.
- Validation passed: `python -m pytest scripts/test_nanopi_rehab_arm_readonly_agent.py scripts/test_nanopi_rehab_arm_collect_and_upload.py -q` (`9 passed`) plus a local collector dry-run using the trained ONNX model. Boundary unchanged: detector output is V evidence only, no CAN, no M33 override, no direct motion permission.
- Continued the rehab-arm mobile App backend toward user-usable closure without changing Stitch/frontend files. `GET /api/rehab-arm/app/v1/public-config` now includes explicit release-gate checks for public config availability, token auth contract, APK frontend API wiring, and missing BLE/M33 hardware protocol packet maps.
- Added authenticated `mobile_readiness_guide` to `GET /api/rehab-arm/app/v1/me`. The guide exposes account onboarding, device binding, training plan, training start gate, APK frontend wiring, hardware protocol, offline queue, and safety-review blockers as phone-renderable evidence only.
- Current release state remains blocked for user download as a real product: the backend is returning honest readiness fields, but the current APK still needs Stitch/frontend login/API wiring and hardware protocol details before it should be described as user-ready.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, existing deprecation warnings only).
- Tightened the NanoPi stereo read-only evidence chain so camera mapping is no longer lost between capture and platform upload. `scripts/nanopi-rehab-arm-readonly-agent.py` now accepts optional `--left-camera-index`, `--right-camera-index`, `--left-flip`, `--right-flip`, and `--flip-applied-before-detection` fields and records them under `stereo_vision_context.capture_loop.camera_mapping`.
- Updated `scripts/nanopi-rehab-arm-collect-and-upload.py` to pass the mapping fields through and to apply requested OpenCV frame flips before saving Python-captured keyframes. Existing file-forwarding remains compatible and records `mapping_state = waiting_physical_index_confirmation` when no index is explicitly supplied.
- Extended `scripts/scan-device-interfaces.py` with a read-only camera scanner. On Linux/NanoPi it enumerates `/dev/video*`, sysfs names, optional `udevadm` properties, optional `v4l2-ctl --list-devices`, and optional `--list-formats-ext`; the readonly agent now preserves these records as `board_manifest.capabilities.camera_device_details` while keeping the legacy `camera_devices` list.
- Live cloud dashboard check for `nanopi-m5` confirmed the current C++ loop still reports `mode=cpp_dual_camera_probe`, `left_flip=hv`, `right_flip=hv`, `applied_before_detection=true`, `baseline_m=0.06`, and no calibration/target/depth/3D coordinate yet. This remains read-only stereo evidence, not motion permission.
- Validation passed: `python -m pytest scripts/test_scan_device_interfaces.py scripts/test_nanopi_rehab_arm_readonly_agent.py scripts/test_nanopi_rehab_arm_collect_and_upload.py -q` (`8 passed`) plus a local stereo dry-run using the latest saved left/right keyframes with `--left-camera-index 0 --right-camera-index 1 --left-flip hv --right-flip hv`.
- Fixed the rehab-arm Stitch V page so live `stereo_left` / `stereo_right` images no longer sit beside static fake detection/calibration copy. The HUD slots now bind to real `stereo_vision_context` fields and show `no_yolo_cup_or_bottle`, waiting end-effector, `depth waiting`, and `waiting_calibration` when the NanoPi payload has no detections, depth, or hand-eye transform.
- Added stable data roles in `apps/web/public/rehab-stitch/vision.html` for left/right frame status, target semantics, target class, end-effector state, visual-lock frames, disparity/depth, and calibration state; the React bridge in `rehab-arm-control-client.tsx` now writes those slots directly instead of relying on fragile label-neighbor replacement.
- Tightened stereo depth extraction so `null` / empty values do not become `0 m`; uncalibrated depth remains waiting instead of appearing as a real metric estimate.
- Validated with `npm --workspace apps/web run build` (passed; one pre-existing hook warning remains around `requestModelRelay`) and local browser QA on `http://127.0.0.1:3027` with cloud API. QA screenshot: `docs/screenshots/rehab-arm-stitch-first-qa/vision-waiting-state-20260703/local-v-page-after-depth-fix.png`; no `GRASP_BOTTLE`, `CALIB_VALID`, fake `98.4%`, or fake `Z:201.88` text remained, and page width matched viewport width at 1600 px.
- Synced only the static `apps/web/public/rehab-stitch/vision.html` file to the cloud host to fix the currently served V page without deploying unrelated dirty React-client work. Cloud QA screenshot: `docs/screenshots/rehab-arm-stitch-first-qa/vision-waiting-state-20260703/cloud-v-page-after-static-fix.png`; the iframe kept real 640x480 left/right images and displayed waiting slots with no fake detection/calibration text or horizontal overflow.
- Confirmed the live NanoPi stereo physical mapping over read-only SSH to `NanoPi-M5` at `192.168.3.36` using password account `pi`. The running C++ upload wrapper command line is `--left /dev/video45 --right /dev/video47 --left-flip hv --right-flip hv`, and the probe argv is `/dev/video45 /dev/video47 ... hv hv`. Platform logical `stereo_left` / `stereo_right` was visually confirmed correct.
- Uploaded a `board_manifest` camera inventory for `nanopi-m5` and patched `/home/pi/rehab_vla/nanopi_vla_cpp_upload_loop.py` on the NanoPi to include `stereo_vision_context.capture_loop.camera_mapping` in every live payload. Latest cloud dashboard validation showed `logical_left.device_path=/dev/video45`, `logical_right.device_path=/dev/video47`, both `flip=hv`, `mapping_state=confirmed_from_nanopi_process_cmdline_and_visual_parallax`, with `target_bottle`, `end_effector`, `candidate_accepted`, and `stable_visual_lock` still live. Depth and `target_3d_camera_frame` correctly remain null until stereo calibration.
- Extended `scripts/nanopi-rehab-arm-stereo-calibration.py` so chessboard capture can use explicit OpenCV camera sources such as `/dev/video45` and `/dev/video47`, while keeping legacy `--left-camera-index` / `--right-camera-index` compatibility. Synced the script to `/home/pi/rehab_arm_stereo_calibration/nanopi-rehab-arm-stereo-calibration.py` on NanoPi.
- NanoPi stereo calibration dry-run passed: `capture-chessboard --left-camera-source /dev/video45 --right-camera-source /dev/video47 --left-flip hv --right-flip hv --sample-count 3` saved three synchronized 640x480 PNG pairs under `/home/pi/rehab_arm_stereo_calibration/samples/source_smoke_20260703`. Running `calibrate` on those non-chessboard samples wrote `/home/pi/rehab_arm_stereo_calibration/calibrations/smoke_waiting_20260703.json` with `calibration_state=waiting` and `valid_pair_count=0`, preserving the no-fake-calibration boundary.
- Captured a real chessboard calibration session at 10-second intervals: `/home/pi/rehab_arm_stereo_calibration/samples/chessboard_real_20260703_01` contains 120 left/right PNG pairs from `/dev/video45` and `/dev/video47` with `hv` flip. Screening with a `9x6` inner-corner pattern found 35 valid stereo pairs and wrote `valid_pairs_9x6.json`; the filtered set is `/home/pi/rehab_arm_stereo_calibration/samples/chessboard_real_20260703_01_valid9x6`.
- Ran stereo calibration on the 35 valid pairs. Unit-square calibration output: `/home/pi/rehab_arm_stereo_calibration/calibrations/chessboard_real_20260703_01_unitsquare_9x6.json`, reprojection error `0.428 px`, left/right reprojection errors about `0.296 px` / `0.289 px`, and baseline `3.138` square units. A provisional meter-scale file normalized to the user-stated approximate `0.06 m` baseline is `/home/pi/rehab_arm_stereo_calibration/calibrations/chessboard_real_20260703_01_baseline06m_provisional_9x6.json`; it derives `square_size_m=0.019120072198` and is marked `provisional_waiting_measured_square_size` until the printed square edge is measured.
- User confirmed the printed Zhang chessboard was the A4 PDF `zhang_chessboard_9x6_inner_20mm_A4.pdf`, so the final metric calibration uses `board_cols=9`, `board_rows=6`, and `square_size_m=0.02`. Final NanoPi outputs are `/home/pi/rehab_arm_stereo_calibration/calibrations/chessboard_real_20260703_01_A4_20mm_9x6.json` and `.yaml`, with `calibration_state=calibrated`, `valid_pair_count=35`, `image_size=640x480`, `baseline_m=0.06276126842552868`, `reprojection_error=0.4277624184705839`, left/right reprojection errors `0.2956263142110364` / `0.28932478904439957`, and `T=[-0.06262379110544022, 0.00028748980333351444, 0.004141853640150551]`.
- Extended the read-only stereo upload path to accept explicit camera sources as well as legacy integer indices. `scripts/nanopi-rehab-arm-collect-and-upload.py` now accepts `--left-camera-source` / `--right-camera-source`, forwards them to `scripts/nanopi-rehab-arm-readonly-agent.py`, and preserves `/dev/video45` / `/dev/video47` as `capture_loop.camera_mapping.*.opencv_source` without inventing `opencv_index=0/1` when only device paths are supplied.
- Synced the updated calibration/read-only upload scripts to NanoPi without restarting the live C++ loop: refreshed `/home/pi/rehab_arm_stereo_calibration/nanopi-rehab-arm-stereo-calibration.py` and added `/home/pi/rehab_arm_stereo_calibration/scripts/nanopi-rehab-arm-readonly-agent.py` plus `nanopi-rehab-arm-collect-and-upload.py`, backing up existing files first. NanoPi dry-run using existing valid chessboard images confirmed `opencv_source=/dev/video45` and `/dev/video47`, null integer indices, `stereo_calibration_id=chessboard_real_20260703_01_A4_20mm_9x6`, `baseline_m=0.06276126842552868`, and `control_boundary=stereo_vision_context_only_not_motion_permission`.
- Added the first read-only calibrated stereo-depth evidence path to `scripts/nanopi-rehab-arm-readonly-agent.py`. It accepts `--stereo-calibration-json`, loads calibrated JSON rectification (`R1/R2/P1/P2`), matches same-label left/right detections, rectifies their centers, computes `disparity_px`, `depth_m`, and `target_3d_camera_frame`, and keeps `target_3d_robot_frame=null` with `transform_state=waiting`. `scripts/nanopi-rehab-arm-collect-and-upload.py` now forwards the calibration JSON path.
- Validation passed locally: `python -m pytest scripts/test_nanopi_rehab_arm_readonly_agent.py scripts/test_nanopi_rehab_arm_collect_and_upload.py -q` (`11 passed`). A unit test verifies `fx=600`, `baseline=0.06`, `disparity=30px` produces `target_3d_camera_frame.z_m=1.2`.
- Synced the updated read-only scripts to NanoPi and ran a dry-run using the final `A4_20mm_9x6` calibration plus existing valid chessboard image pair. The payload correctly reported `stereo_depth_evidence.state=waiting_stereo_match`, `estimated_depth_m=null`, `target_3d_camera_frame=null`, `transform_state=waiting`, and `target_3d_robot_frame=null`, because the chessboard frame has no same-label target/end-effector detector pair.
- Added `scripts/rehab-arm-stereo-depth-evidence.py`, a standalone offline enrichment tool for existing `stereo_vision_context` JSON. It reads a context payload plus calibration JSON, matches same-label left/right detections, computes calibrated depth when possible, and otherwise preserves explicit waiting/null fields. Validation passed with `python -m pytest scripts/test_rehab_arm_stereo_depth_evidence.py scripts/test_nanopi_rehab_arm_readonly_agent.py scripts/test_nanopi_rehab_arm_collect_and_upload.py -q` (`13 passed`).
- Pulled the latest cloud `nanopi-m5` stereo payload from the dashboard API for a no-upload enrichment dry-run. Current live payload still has `stereo_calibration_id=""`, `detections.left=[]`, `detections.right=[]`, and no target/end-effector objects, so the enrichment tool correctly produced `stereo_depth_evidence.state=waiting_stereo_match` with `estimated_depth_m=null`, `target_3d_camera_frame=null`, and `target_3d_robot_frame=null`.
- NanoPi SSH became unavailable during this slice (`Error reading SSH protocol banner` after repeated attempts), so `scripts/rehab-arm-stereo-depth-evidence.py` was not synced to the board and the live C++ wrapper was not changed. Next hardware step is to restore SSH or reboot/check load, then sync the tool and add right-eye detection to the live wrapper.
- After NanoPi power-cycle/SSH recovery, patched `/home/pi/rehab_vla/nanopi_vla_cpp_upload_loop.py` with a backup at `/home/pi/rehab_vla/nanopi_vla_cpp_upload_loop.py.bak_stereo_depth_20260703_1148`. The live wrapper now runs target YOLO on both `latest_left.jpg` and `latest_right.jpg`, uploads `detections.right`, loads `chessboard_real_20260703_01_A4_20mm_9x6.json`, emits `stereo_depth_evidence`, keeps `transform_state=waiting`, and never fills `target_3d_robot_frame` without hand-eye calibration. A repo copy of the patched wrapper was saved at `scripts/nanopi-vla-cpp-upload-loop.py`.
- Updated the platform API schema so cloud `stereo_vision_context` preserves `right_target_quality_gate`, `end_effector_quality_gate`, `stereo_depth_evidence`, `camera_to_robot_transform`, `transform_state`, and `target_3d_robot_frame`. Deployed `apps/api/app/modules/rehab_arm/schemas.py` to the cloud and restarted API/Web; health checks passed.
- Real target attempt with the user's placed bottle: cloud payload now reports `stereo_calibration_id=chessboard_real_20260703_01_A4_20mm_9x6`, `baseline_m=0.06276126842552868`, and right-eye `target_bottle` detections around `0.5` confidence. A temporary `--target-conf 0.20` test produced same-label left/right detections, but the left bbox locked onto the bottle base/table reflection while the right bbox covered the bottle body, causing `stereo_depth_evidence.state=rejected`, `reason=rectified_vertical_mismatch`, and vertical delta about `125 px`. The quality gate correctly kept `estimated_depth_m`, `target_3d_camera_frame`, and `target_3d_robot_frame` null. The loop was restored to `--target-conf 0.30` and verified as a single running wrapper process.
- User replaced the target detector with `/home/pi/rehab_arm_models/yolo/yolo11s.onnx` and the live wrapper now runs with `--target-conf 0.45`. Cloud evidence became stable: left/right both report `target_bottle`, rectified vertical mismatch stayed around `0.7-2.3 px`, `stereo_depth_evidence.state=accepted`, disparity about `47-48 px`, and depth about `0.595-0.603 m`.
- Captured the first accepted stereo-depth evidence set under `docs/screenshots/rehab-arm-stitch-first-qa/stereo-calibration-20260703/`: `latest_left.jpg`, `latest_right.jpg`, `latest_platform_context.json`, `latest_target_context.json`, `latest_target_context_right.json`, and `stereo-depth-evidence-summary.json`. The summary records `target_3d_camera_frame={x_m:-0.08624,y_m:0.04604,z_m:0.60219}` for `target_bottle`, with `camera_to_robot_transform=null`, `transform_state=waiting`, and `target_3d_robot_frame=null`.
- Stereo-depth next gate: integrate the final `A4_20mm_9x6` calibration into the live NanoPi/C++ OpenCV perception loop for rectification, disparity, `disparity_px`, `depth_m`, and `target_3d_camera_frame`. `target_3d_robot_frame` must remain `null` / `waiting` until a separate `camera_to_robot_transform` is calibrated; this calibration does not grant IK or motion permission by itself.
- Added the rehab-arm IK dry-run evidence loop. Backend now accepts `rehab_arm_ik_candidate_request_v1` at `/api/rehab-arm/v1/devices/{device_id}/ik-candidates` and `/api/rehab-arm/v1/projects/{project_id}/devices/{device_id}/ik-candidates`, stores latest `ik_candidate`, and returns `rehab_arm_ik_candidate_evidence_v1`.
- The IK response includes `ik_status`, `candidate_joint_trajectory`, `joint_limit_check`, `collision_or_workspace_check`, `simulation_result`, forbidden dangerous outputs, and `control_boundary = ik_candidate_evidence_only_not_motion_permission`.
- The `3D / 孪生` Stitch page now exposes manual `robot_frame` coordinate input, source selection, optional approach/orientation fields, IK candidate generation, status display, and local evidence JSON export without adding any real-motion control.
- Historical note from the first IK bring-up: the platform `ARM_JOINT_MAP` once listed `zhou_zongxiang_joint` before `jian_xuanzhuan_joint`; after MuJoCo shadow alignment, new platform-generated `robot_render_state_v1` follows the MuJoCo order `jian_hengxiang_joint`, `jian_zongxiang_joint`, `jian_xuanzhuan_joint`, `zhou_zongxiang_joint`, `wanbu_zongxiang_joint`, and `wanbu_hengxiang_joint`. Consumers should still map by joint name for compatibility with older snapshots.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_sync.py -q` from `apps/api` (`43 passed`), `npx --workspace apps/web tsc --noEmit`, and `npm --workspace apps/web run build`.
- Build note: the web build still reports one existing React hook warning for `requestModelRelay` inside the Stitch iframe updater. The IK-specific dependency warning was fixed; wrapping `requestModelRelay` safely requires moving it after dry-run derived state or refactoring the iframe command bridge.
- Remote sim host check: `192.168.3.34` responds to ping and TCP/22. Follow-up login with `cal` succeeded read-only. Host is Ubuntu 24.04.4 LTS (`cal-MS-7D90`), service `rehab-arm-sim-host-shadow.service` is active, and it launches `rehab_arm_sim_mujoco medical_arm_6dof_hardware_shadow.launch.py`.
- Remote sim host inventory: ROS environment uses `ROS_DOMAIN_ID=42`, `ROS_LOCALHOST_ONLY=0`, and `ROS_AUTOMATIC_DISCOVERY_RANGE=SUBNET`. Active nodes are `/medical_arm_6dof_shadow_sim`, `/medical_arm_shadow_relay`, and `/rehab_arm_psoc_bridge`. Key topics include `/joint_states`, `/arm_controller/joint_trajectory`, `/sim/medical_arm/joint_states`, `/sim/medical_arm/joint_trajectory`, `/sim/medical_arm/safety_state`, and `/sim/medical_arm/sensor_state`.
- Remote model facts: the 6DOF shadow joint list is `jian_hengxiang_joint`, `jian_zongxiang_joint`, `jian_xuanzhuan_joint`, `zhou_zongxiang_joint`, `wanbu_zongxiang_joint`, `wanbu_hengxiang_joint`; limits are `[-0.7854,1.5708]`, `[-0.5236,1.7453]`, `[-1.0472,1.0472]`, `[0.0,2.3562]`, `[-0.7854,0.7854]`, and `[-0.3491,0.5236]` respectively. A read-only echo of `/sim/medical_arm/joint_states` returned live positions.
- Cloud deployment completed by copying the IK API/web/Stitch runtime files to `ubuntu@106.55.62.122:/home/ubuntu/apps/ai-collab`, running `npm run build:web`, and restarting with `RESTART=1 scripts/start-cloud-prod.sh`; API `:8011` and Web `:3001` health checks passed.
- Cloud runtime validation: direct HTTP `POST http://106.55.62.122:8011/api/rehab-arm/v1/devices/nanopi-m5/ik-candidates` returned `candidate_ready`, joint names, joint-limit rows, workspace check, latest simulation readiness, forbidden outputs, and the IK evidence-only boundary.
- Cloud browser QA passed with `node scripts/validate-rehab-arm-ik-loop-cloud.mjs`; evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/ik-loop-20260703/`. The QA opened the real cloud page, switched to `/rehab-stitch/twin.html`, entered robot-frame coordinates without a vision target, generated an IK candidate, exported `ik-candidate-evidence-export.json`, and found no direct motion/CAN/estop-release/M33-override button text.
- Continued the IK path toward MuJoCo-backed inverse kinematics. The platform IK candidate response now uses the remote `medical_arm_6dof` MuJoCo shadow joint limits instead of the earlier coarse `[-1.57,1.57]` placeholders.
- Added `mujoco_shadow_validation_plan` to IK candidate evidence. The plan names `192.168.3.34`, `ROS_DOMAIN_ID=42`, `/sim/medical_arm/joint_trajectory`, `/sim/medical_arm/joint_states`, and `mujoco_shadow_dry_run_only_not_motion_permission`; it explicitly forbids `/arm_controller/joint_trajectory`, `/joint_states`, CAN, and motor bus publication.
- Updated the `3D / 孪生` Stitch-bound IK panel to display the shadow target topic and `sim_only_shadow_topic_not_hardware_chain` scope, and added the shadow validation plan to the A-field candidate export.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_sync.py -q` from `apps/api` (`43 passed`), `npx --workspace apps/web tsc --noEmit`, and `npm --workspace apps/web run build`.
- Cloud deploy/QA passed after the MuJoCo contract update. The cloud API returned `jian_hengxiang_joint [-0.7854,1.5708]`, `/sim/medical_arm/joint_trajectory`, `/sim/medical_arm/joint_states`, and forbidden `/arm_controller/joint_trajectory`; browser QA refreshed `docs/screenshots/rehab-arm-stitch-first-qa/ik-loop-20260703/` and exported JSON with `mujoco_shadow_validation_plan`.
- Added `scripts/validate-rehab-arm-mujoco-shadow-candidate.py` as the first remote MuJoCo shadow execution runner. It validates the exported IK evidence boundary, refuses hardware topics, publishes once to `/sim/medical_arm/joint_trajectory`, echoes `/sim/medical_arm/joint_states`, and writes `mujoco_shadow_execution_evidence_only_not_motion_permission` JSON.
- Ran the shadow runner against `192.168.3.34` with the latest cloud-exported IK candidate. It published positions `[-0.112, 0.088, 0.352, 0.0, 0.0, 0.0]` to the sim-only topic, and read back live shadow positions including approximately `[-0.1030, 0.0810, 0.3238, 0.0, 0.0, 0.0]`. Evidence is `docs/screenshots/rehab-arm-stitch-first-qa/ik-loop-20260703/mujoco-shadow-execution-evidence.json`.
- Boundary: IK candidates are only A-field dry-run evidence. Real motion remains `L -> V -> A -> MuJoCo/URDF -> M33 final safety decision -> NanoPi/M33 hardware chain`; NanoPi is not allowed to bypass M33 or send direct CAN/motor control for these candidates.
- Improved the IK candidate generator from coarse heuristic setpoints to a MuJoCo-shadow-aligned numerical solver. Backend FK now uses the remote `medical_arm_6dof_shadow.xml` nesting order and offsets (`jian_hengxiang -> jian_zongxiang -> jian_xuanzhuan -> zhou_zongxiang -> wanbu_zongxiang -> wanbu_hengxiang`) while still returning positions in the platform/shadow `joint_names` array.
- IK evidence now includes `ik_solver_report` with `method=damped_least_squares_fk_numeric_jacobian_mujoco_6dof_v1`, `position_error_m`, end-effector position, iteration count, convergence flag, and `quality=precise|approximate|blocked`. `ik_status` is residual-based: `candidate_ready`, `candidate_approximate`, or `candidate_blocked`.
- The Stitch-bound `3D / 孪生` IK panel now displays solver quality and position error, and the exported A-field JSON includes `ik_solver_report`. This remains `ik_candidate_evidence_only_not_motion_permission` and does not add any true-motion command.
- Read-only SSH to `192.168.3.34` confirmed the MuJoCo XML facts used by the solver: child body/site offsets are `0.24, 0.18, 0.28, 0.12, 0.10, 0.10 m`; platform render order differs from MuJoCo nesting order for `zhou_zongxiang_joint` and `jian_xuanzhuan_joint`, so FK must map by joint name.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_sync.py -q` from `apps/api` (`43 passed`), `npx --workspace apps/web tsc --noEmit`, and `npm --workspace apps/web run build` (passed with the existing `requestModelRelay` hook warning).
- Deployed the numerical IK slice to cloud by copying only the scoped runtime/docs files, then running `npm run build:web` and `RESTART=1 bash scripts/start-cloud-prod.sh`. Cloud health checks passed for API `:8011` and Web `:3001`; build still reports only existing React hook warnings outside this IK boundary.
- Cloud runtime validation passed: `POST http://106.55.62.122:8011/api/rehab-arm/v1/devices/nanopi-m5/ik-candidates` for target `{x:0.4,y:-0.1,z:0.05}` returned `candidate_ready`, `ik_solver_report.method=damped_least_squares_fk_numeric_jacobian_mujoco_6dof_v1`, `position_error_m=0.025`, `quality=precise`, and `control_boundary=ik_candidate_evidence_only_not_motion_permission`.
- Cloud browser QA passed with `node scripts/validate-rehab-arm-ik-loop-cloud.mjs --output-dir docs/screenshots/rehab-arm-stitch-first-qa/ik-loop-20260703-numeric`. Evidence files: `01-page-open.png`, `02-robot-frame-input.png`, `03-ik-candidate-ready.png`, `ik-candidate-evidence-export.json`, and `qa-metrics.json`. The default manual target produced `candidate_approximate`, limit `pass`, workspace `reachable`, evidence-only export boundaries, and `dangerousMatches=[]`.
- Continued the rehab-arm mobile backend closed loop without changing Stitch/frontend files. Added durable diagnostic uploads, offline queue items, and platform sync run records to the App backend.
- Added `/api/rehab-arm/app/v1/devices/{device_id}/diagnostic-upload` and `/diagnostics` so phone/M33 diagnostic snapshots can update device heartbeat evidence and be reviewed later.
- Added evidence-only offline queue endpoints: `POST /offline-queue`, `GET /offline-queue`, and `POST /offline-queue/replay`. Replay is explicitly whitelisted to diagnostic upload, training session progress, EMG summary, M55 intent summary, and platform sync; motor/CAN-like operations return `OFFLINE_OPERATION_NOT_ALLOWED`.
- Added platform sync run history and safety audit reads: `/platform/sync-runs` and `/safety-audit`. Safety audit includes human App actions and bound M33 identity decisions so rejected/accepted sync decisions are visible to the phone workflow.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`4 passed`). Initial full-test run timed out because a stale pytest process remained after a previous timeout; after stopping stale pytest processes the same test file passed.
- Added the post-session training report loop: `POST /training-sessions/{session_id}/report`, `GET /training-sessions/{session_id}/report`, `GET /training-reports`, and `GET /training-reports/{report_id}`. Reports are generated only after a session is `finished`, aggregate session completion, EMG summaries, M55 intent summaries, M33 safety evidence, and recommendations for therapist/app review.
- `GET /api/rehab-arm/app/v1/me` now includes `latest_report` so the phone App can show the most recent review result on bootstrap. Report responses explicitly use `training_report_review_only_not_medical_diagnosis_or_motion_permission` and do not grant movement permission.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`4 passed`, deprecation warnings only).
- Added the BLE message contract backend loop without changing Stitch/frontend files. New endpoints are `POST /devices/{device_id}/ble/messages`, `GET /devices/{device_id}/ble/messages`, and `POST /devices/{device_id}/ble/messages/{message_id}/ack`.
- BLE messages generate `rehab_app_ble_v1` payloads for whitelisted App-to-M33 message types only: App hello, device status request, training plan push, session start/progress/pause/stop request, and diagnostic snapshot request. Payload and ACK validation rejects motor/CAN/raw motion/M33 override/emergency-stop release fields.
- Added `RehabAppBleMessage` persistence, SQLite self-healing, audit logging, and test coverage for structured `training_plan_push`, M33 ACK evidence, and unsafe payload rejection.
- Fixed latest EMG/M55 ordering stability by writing application-layer UTC timestamps for summary records instead of relying only on SQLite second-precision defaults.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`4 passed`, deprecation warnings only).
- Added the training report review loop: `POST /training-reports/{report_id}/reviews` and `GET /training-reports/{report_id}/reviews`. Reviews record reviewer role, status, note, next-step intent, new-plan request flag, and follow-up payload as evidence only.
- Training report reads, lists, and `/me` bootstrap now include `latest_review`, and platform sync summaries can count `training_report_reviews`.
- Preserved the report boundary: reviews are not medical diagnosis, treatment authorization, or motion permission, and any requested plan still has to become a training plan, sync to M33, and receive current-version `m33_accepted`.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`4 passed`, deprecation warnings only).
- Added the report-review-to-next-draft bridge: `POST /training-reports/{report_id}/draft-next-plan` creates an AI draft from the finished report, latest review, original plan, EMG overview, and M55 intent overview. It can reduce sets/reps and slightly increase assist when review/pain/fatigue indicates adjustment, but it remains `ai_draft_only_not_execution_permission`.
- Verified that accepting the generated draft creates a normal plan and that starting it immediately is blocked with `M33_ACCEPTANCE_REQUIRED` until the new plan version is synced and accepted by M33.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`4 passed`, deprecation warnings only).
- Extended platform sync summaries with `ai_training_drafts`, so report-derived next-plan drafts can be counted as cloud/platform evidence without becoming motion permission.
- Added AI draft recovery for the phone workflow: `GET /ai-training-drafts?status=all|open|accepted` lists persisted drafts, and `/me` now includes `latest_open_ai_draft` so an unfinished AI plan review can resume after app restart.
- Tightened the App training-session state machine: `training-sessions/start` now rejects duplicate active sessions on the same device with `ACTIVE_TRAINING_SESSION_EXISTS`, while allowing a new session after the previous one is finished.
- Locked finished App training sessions against later progress or finish mutation. `PATCH /training-sessions/{id}/progress` and repeated finish calls now return `TRAINING_SESSION_NOT_ACTIVE` after a session is finished.
- Locked report source evidence after report generation. Repeated report POSTs now return the existing report, while late EMG/intent uploads for the reported session return `TRAINING_REPORT_ALREADY_GENERATED`.
- Added revoked-device enforcement. `trust_status=revoked` devices can still be viewed and receive diagnostic uploads, but plan sync, BLE messages/ACKs, M33 status updates, and session start now return `DEVICE_REVOKED`.
- Reworked the rehab-arm command-center AI model section into an inline control-room module instead of a `/model-relay-lab` jump. Stitch MCP generated the reference desktop screen `projects/1207434422396149303/screens/3a7d99e6dc1845fab531bec42db44392` (`AI模型 / 模型中转内联控制台`), and the live page now exposes `ai_model` navigation alongside URDF/3D and EMG.
- Preserved existing model relay functions in the inline module: provider/model/base URL/API key save, high-level prompt request, model suggestion/result display, restricted device token generation/copy, HTTP/XiaoZhi endpoints, curl example, forbidden-output list, and relay event audit stream.
- Removed visible control-room jumps to `/model-relay-lab`; training and VLA-L surfaces now switch to the same-page AI model module while data/labeling still links to the robotics data workbench.
- Validation passed: `npm --workspace apps/web run build`. Boundary remains unchanged: browser AI output is suggestion/dry-run evidence only and does not grant CAN, raw motor, current, torque, position/velocity, M33 override, or emergency-stop release authority.
- Added a real App device unbind/freeze lifecycle: `POST /api/rehab-arm/app/v1/devices/{device_id}/unbind` marks the binding `revoked`, keeps history/status/diagnostics readable, writes an audit log, and returns `device_unbound_history_retained_not_motion_permission`.
- Verified that unbound devices cannot sync training plans, create BLE messages, update M33 sync decisions, or start training sessions, while post-unbind diagnostic upload remains allowed for evidence. Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`5 passed`, deprecation warnings only).
- Added App training-session pause/resume/cancel endpoints: `POST /training-sessions/{session_id}/pause`, `/resume`, and `/cancel`. Paused sessions still block a new session on the same device; progress writes require an active started/in-progress session; cancelled sessions are ended and locked from finish/report/progress mutation.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`6 passed`, deprecation warnings only). This is still record/state-machine evidence only and does not send pause/stop motor commands.
- Tightened training-plan lifecycle safety: archived or rejected plans now return `TRAINING_PLAN_NOT_USABLE` when the phone App tries to sync them to M33 or start a training session. Draft/active plans keep the existing M33 acceptance flow.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`6 passed`, deprecation warnings only).
- Added training preflight checks as a real backend gate before session start. New `rehab_app_preflight_checks` records bind user, plan, device, M33 sync id, plan version, pain-before score, and checklist evidence; `POST /api/rehab-arm/app/v1/training-preflight` requires device-worn, pain-within-limit, stop-explained, and M33-plan-accepted checklist items.
- `training-sessions/start` now requires a current passed preflight matching the latest accepted M33 sync and plan version; stale/missing preflight returns `PREFLIGHT_CHECK_REQUIRED`. Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`6 passed`, deprecation warnings only).
- Added App restart recovery support for the preflight/session loop: `/me` now returns `latest_preflight` and treats paused sessions as recoverable `active_session`, while `GET /training-preflight` lists recent checks with optional `plan_id` and `device_id` filters.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`6 passed`, deprecation warnings only).
- Added a pain/contraindication gate to App preflight: patient-submitted `pain_before` now returns `PREFLIGHT_PAIN_REVIEW_REQUIRED` when it is at least `pain_baseline + 2` or at least `7`, while a therapist preflight can record reviewed evidence for the same plan/device/sync. This remains workflow safety evidence, not diagnosis or motion permission.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`6 passed`, deprecation warnings only).
- Added profile medical-constraint enforcement before plan sync and session start. Plans that conflict with recorded constraints such as `no overhead motion` now return `TRAINING_PLAN_CONTRAINDICATED` unless the plan carries therapist review evidence in `safety_constraints.therapist_constraint_reviewed=true`; reviewed plans still require normal M33 acceptance and preflight.
- Validation passed after one test assertion adjustment for same-second preflight ordering: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`6 passed`, deprecation warnings only).
- Replaced the ad-hoc `safety_constraints.therapist_constraint_reviewed` bypass with durable plan constraint review records. New `rehab_app_plan_constraint_reviews` evidence is created through `POST /training-plans/{plan_id}/constraint-reviews`, listed through `GET /training-plans/{plan_id}/constraint-reviews`, and must match the current plan version before a contraindicated plan can sync/start.
- Validation passed: `python -m pytest tests/test_rehab_arm_app_backend.py -q` from `apps/api` (`6 passed`, deprecation warnings only).
- Added in-session safety event evidence: `POST /training-sessions/{session_id}/safety-events` and `GET /training-sessions/{session_id}/safety-events` persist pain/device-fit/M33/fatigue/manual-stop events. Critical events or pain reports with score >= 7 automatically move an active session to `paused` and increment interruption evidence.
- Training reports now include safety event counts, critical event counts, event types, and max in-session pain score, with review recommendations for critical or high-pain events. Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only); a plain rerun timed out once before the faulthandler run completed.
- Added platform/offline coverage for the new evidence loops: platform sync now counts `plan_constraint_reviews` and `session_safety_events`, and offline replay can submit `plan_constraint_review` plus `session_safety_event` items through the existing evidence-only queue.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only).
- Added safety-review gating after critical in-session events. A paused session with an unreviewed critical safety event now returns `SAFETY_REVIEW_REQUIRED` on resume; cancelled/finished sessions with unreviewed critical events also block the next start on the same device until a `safety_review` event with approved/conditional status is recorded.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only).
- Added a read-only App training readiness endpoint: `GET /api/rehab-arm/app/v1/training-plans/{plan_id}/readiness?device_id=...`. It summarizes start blockers for plan usability, revoked devices, profile contraindications, active sessions, M33 acceptance, preflight evidence, and pending safety reviews so the phone UI can show clear reasons before calling start.
- The readiness response returns `can_start`, per-check status/code/detail, and `training_readiness_evidence_only_not_motion_permission`; it does not grant robot motion authority. Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only).
- Added `GET /api/rehab-arm/app/v1/training-plans/{plan_id}/start-guide?device_id=...` as a read-only phone workflow guide. It reuses readiness checks and returns `next_action` plus ordered steps for selecting a usable plan, rebinding a trusted device, therapist constraint review, active-session recovery, M33 sync/acceptance, preflight, safety review, or final App session start.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide remains `training_start_guide_evidence_only_not_motion_permission` and does not create sessions or send hardware commands.
- Added `/me.primary_start_guide` so the phone home screen can render one recommended training action from the latest usable plan and trusted device without duplicating readiness/start-guide composition in the frontend. It still surfaces active-session recovery before new starts and remains evidence-only.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only).
- Added `/me.onboarding_guide` for first-run phone users. It returns ordered setup steps for profile, trusted M33 device binding, and usable training plan creation/acceptance, with endpoint/method/payload hints and `app_onboarding_guide_evidence_only_not_motion_permission`.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only).
- Closed the rehab-arm Stitch diagnostics page action loop. The `IO / 诊断` page now binds `刷新状态`, `导出诊断`, and `查看日志` inside the Stitch iframe while preserving the Stitch visual page.
- `刷新状态` calls the existing live dashboard refresh and shows transient button feedback; `导出诊断` downloads a read-only JSON snapshot with device, L/V/A, simulation, M33 safety, CAN/wiring, and recent event evidence; `查看日志` switches to the same-level `LOG / 日志` module.
- Deployed the rehab-arm control client and diagnostics Stitch page to the cloud host; `npm run build:web && RESTART=1 scripts/start-cloud-prod.sh` passed, with only pre-existing React hook warnings in `project-playable-shell.tsx` and model-relay helper functions.
- Cloud user-view QA passed on `http://106.55.62.122:3001/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control`: diagnostics refresh feedback appeared, JSON download succeeded, the logs button switched to `/rehab-stitch/logs.html`, and no horizontal overflow was detected. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/diagnostics-actions-20260703/`.
- Boundary: diagnostics export is browser-local evidence only. It does not send CAN, motor, torque/current, raw pose/velocity, M33 override, or emergency-stop release commands, and it does not change XiaoZhi/L transport.
- Added `/me.daily_action_guide` for the rehab-arm phone App. It prioritizes active-session recovery, open AI draft review, latest-report review, report-to-next-plan draft generation, primary training start guidance, and onboarding fallback so the home screen has one backend-authored daily action.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide is `app_daily_action_guide_evidence_only_not_motion_permission`.
- Added `/me.care_timeline` for the phone App history surface. It aggregates recent training sessions, training reports, AI training drafts, and queued offline evidence items into an evidence-only timeline with source ids, statuses, timestamps, and compact detail payloads.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The timeline is `app_care_timeline_evidence_only_not_motion_permission`.
- Added `/me.care_summary` for the phone App home overview. It summarizes active/finished/cancelled sessions, report and pending-review counts, open AI drafts, queued offline evidence, start readiness, and blockers such as onboarding, active session, report review, AI draft, or offline queue.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The summary is `app_care_summary_evidence_only_not_motion_permission`.
- Added `/me.offline_sync_guide` so the phone App can explain queued/replayed/failed offline evidence, provide queued item ids, and show the safe replay payload for `POST /offline-queue/replay` without treating offline replay as a command bus.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide is `offline_sync_guide_evidence_only_not_motion_permission`.
- Added `/me.session_recovery_guide` for active or paused phone training workflows. It lists evidence-state actions for view/progress/finish/cancel/resume, and switches paused sessions with unreviewed critical safety events to a required safety-review action instead of direct resume.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide is `session_recovery_guide_evidence_only_not_motion_permission`.
- Added `/me.report_followup_guide` for post-training phone workflows. It guides report review, next-plan AI draft generation, open draft acceptance, and accepted-plan M33 sync from persisted report/draft state without granting motion permission.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide is `report_followup_guide_evidence_only_not_motion_permission`.
- Added `/me.device_operational_guide` for the phone device card. It guides bind/diagnostic upload/plan sync/M33 decision pending/M33 rejection review/accepted-readiness states from persisted device, diagnostic, and sync evidence without sending motor or CAN commands.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide is `device_operational_guide_evidence_only_not_motion_permission`.
- Added `/me.safety_review_guide` for historical critical safety blockers. It points the phone App to the exact session/event needing approved/conditional `safety_review`, and daily action now prioritizes this blocker when no active session exists.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide is `safety_review_guide_evidence_only_not_motion_permission`.
- Added `/me.accepted_plan_guide` for AI drafts that have been accepted into normal training plans. It routes the phone through trusted-device binding, M33 sync/decision, preflight/readiness, and start-record creation without treating AI acceptance as motion permission.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`6 passed`, deprecation warnings only). The guide is `accepted_plan_guide_evidence_only_not_motion_permission`.
- Added `/me.home_status_guide.secondary_actions` so the first phone card can expose follow-up actions from the same backend source guide after deduping the primary action. Failed offline evidence now shows `VIEW_OFFLINE_QUEUE` as the primary action and `REVIEW_FAILED_OFFLINE_ITEM` as a secondary action with the exact review endpoint.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). The home card remains `app_home_status_guide_evidence_only_not_motion_permission`.
- Closed the Stitch `A / 动作规划` page action loop. The page now keeps the Stitch visual design, shows the current L/V/A dry-run candidate JSON, and binds `导出候选`, `查看证据`, `保持 Dry-run`, and `刷新候选`.
- `导出候选` downloads an `a_field_candidate_export_only_not_motion_permission` JSON snapshot; `查看证据` switches to the `LOG / 日志` module; `保持 Dry-run` only shows a local read-only hold confirmation; `刷新候选` reuses the existing live dashboard refresh.
- Deployed the planner Stitch page and rehab-arm control client to the cloud. Cloud build/restart passed, with only pre-existing hook warnings. Browser QA evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/planner-actions-20260703/`; exported candidate JSON was inspected and the logs transition reported no horizontal overflow.
- Closed the Stitch `L / 模式路由` page action loop. The sidebar icons now use the unified module router, and the page binds `进入模式`, `导出路由`, and `刷新`.
- The router page maps semantic modes to shared resources instead of siloed pages: fetch/vision-servo -> `A / 动作规划`, training -> `TRN / 训练`, assistive EMG -> `EMG / 肌电`, diagnostics/safety-review -> `IO / 诊断`, data collection -> `DATA / 数据`, chat -> `LOG / 日志`. Exported route snapshots use `semantic_route_evidence_only_not_motion_permission`.
- Deployed the router Stitch page and rehab-arm control client to the cloud. Cloud QA on the current XiaoZhi chat semantic state confirmed the page showed the L summary, exported a route JSON with `routed_module=logs`, refreshed successfully, switched to `/rehab-stitch/logs.html`, and had no horizontal overflow. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/router-actions-20260703/`.
- Closed the Stitch `TRN / 训练` page action loop. The page now keeps the Stitch training-track visual while binding `刷新训练库`, `查看 AI 摘要`, `导出训练日志`, and `查看 APP 训练库`.
- The exported training chain uses `training_orchestration_evidence_only_not_motion_permission` and documents the competition-demo architecture: XiaoZhi/L triggers training, the platform routes and reviews, the phone APP owns the training library, APP-side AI can fill plans from M55 summaries, M55 provides four-channel EMG intent/fatigue evidence, and M33 pulls targets/constraints over BLE as final safety authority.
- Cloud QA passed: the training page refreshed, exported a valid training-chain JSON, switched to the inline AI model page, opened the phone PWA training-library route in a new page, and showed no horizontal overflow. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/training-actions-20260703/`.
- Closed the Stitch `EMG / 肌电助力` page action loop. The page now exposes unified module navigation plus `刷新`, `导出`, `训练链`, and `日志` controls while keeping the approved Stitch muscle/EMG visual.
- The EMG export uses `emg_assist_evidence_only_not_motion_permission` and captures semantic mode, M55 inference candidates, four-channel EMG rows, training-link relation, and M33 safety boundary. When real EMG telemetry is absent, the exported channels remain `unknown` instead of pretending fixed microvolt values are real.
- Cloud QA passed: the muscle page refreshed, exported the EMG-assist evidence JSON, switched to `TRN / 训练`, switched to `LOG / 日志`, and showed no horizontal overflow. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/muscle-actions-20260703/`.
- Closed the Stitch `3D / 数字孪生` page action loop. The page now has unified module navigation plus `刷新`, `导出证据`, `A 规划`, and `日志` actions while keeping the Stitch 3D/URDF stage and the existing React/Three.js URDF portal.
- The digital-twin export uses `digital_twin_evidence_only_not_motion_permission` and captures `robot_render_state_v1`, fresh/stale/clamped joint counts, pose samples, MuJoCo shadow state, VLA target/end-effector context, model-package metadata, and M33 safety boundary.
- Cloud QA passed: the twin page rendered a nonblank canvas inside `#urdf-runtime-stage`, refreshed, exported a valid twin evidence JSON, switched to `A / 动作规划`, switched to `LOG / 日志`, and showed no horizontal overflow. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/twin-actions-20260703/`.
- Closed the Stitch `V / 视觉` page action loop. The page now has unified module navigation plus `刷新视觉`, `导出证据`, `A 规划`, and `日志` actions while preserving the rule that boxes/annotations come from backend keyframes, not frontend fake drawing.
- The vision export uses `vision_evidence_only_not_motion_permission` and captures left/right keyframe availability and URLs, XiaoZhi/L summary, semantic mode, target/end-effector labels, visual state, pixel error, visual-lock progress, target quality, camera-to-robot calibration readiness, A dry-run gate, and M33 safety boundary.
- Cloud QA passed: the vision page displayed both backend stereo keyframes, refreshed, exported a valid vision evidence JSON, switched to `A / 动作规划`, switched to `LOG / 日志`, and showed no horizontal overflow. Current evidence truthfully reports frames available but target/end-effector not locked. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/vision-actions-20260703/`.
- Finalized the Stitch `AI / 模型` same-level module around `apps/web/public/rehab-stitch/model-lab.html`. It now keeps the Stitch visual shell while binding provider/model/base URL/API key save, prompt testing, endpoint/token copy helpers, scoped relay token generation, filtered relay terminal output, recent model relay events, and local evidence export.
- The AI model evidence export uses `model_relay_evidence_only_not_motion_permission` and captures provider state, L semantic summary, target/mode, filtered relay response, recent relay events, endpoints, token-generated state without token plaintext, and M33 safety boundary.
- Cloud QA passed after deployment: `/rehab-stitch/model-lab.html` loaded as the `AI / 模型` iframe, copy-value wiring populated the WS endpoint, the event stream showed relay boundary and filtered output, and `导出证据` existed. Evidence screenshots include `docs/screenshots/rehab-arm-stitch-first-qa/ai-model-module-stitch/cloud-ai-model-event-stream-1600.png`, `cloud-ai-model-copy-value-1600.png`, and `cloud-ai-model-evidence-export-1600.png`.
- Follow-up QA downloaded the AI model evidence JSON from the cloud page and inspected it at `docs/screenshots/rehab-arm-stitch-first-qa/ai-model-module-stitch/downloaded-ai-model-evidence.json`; it contained provider, semantic, relay, endpoint, and safety-boundary sections, reported `model_relay_evidence_only_not_motion_permission`, and did not include relay token plaintext. Screenshot: `cloud-ai-model-evidence-download-1600.png`.
- Updated architecture docs `docs/AI_PROJECT_STRUCTURE_GITHUB.md` and `docs/rehab-arm-nanopi-vla-mujoco-integration.md` to record that `AI / 模型` is a same-level control-room module, while `/model-relay-lab` remains the advanced/full laboratory.
- Closed the Stitch `LOG / 日志` page action loop. The page now keeps the Stitch audit-stream layout while binding `刷新`, `导出`, raw-packet copy, and quick jumps to `V / 视觉`, `AI / 模型`, and `IO / 诊断`.
- The log export uses `logs_evidence_export_only_not_motion_permission` and captures current L/V/A context, left/right frame availability, dry-run gate state, M33 final-authority boundary, and the recent event stream. The browser still cannot send CAN, motor, torque/current, raw pose/velocity, M33 override, or emergency-stop release commands.
- Deployed `apps/web/app/projects/[id]/rehab-arm-control/rehab-arm-control-client.tsx` and `apps/web/public/rehab-stitch/logs.html` to the cloud. Cloud build/restart passed, with only pre-existing React hook warnings.
- Cloud QA passed on `http://106.55.62.122:3001/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control`: the `LOG / 日志` iframe loaded `/rehab-stitch/logs.html`, refresh feedback appeared, raw-packet copy was available, evidence JSON downloaded with 32 events, and no horizontal overflow was detected at 1600 px. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/logs-actions-20260703/`.
- Promoted `DATA / 数据` from a sidebar jump into a same-level rehab control-room module. Added `apps/web/public/rehab-stitch/data.html` and routed `data_hub` through the unified Stitch iframe navigation while preserving the full `/robotics?tab=dataset` workbench entry.
- The DATA page now shows capture sources, left/right stereo sample frames, target/end-effector label readiness, annotation/training pipeline, raw read-only packet, refresh, copy, export, and quick jumps to `V / 视觉`, `AI / 模型`, and `LOG / 日志`.
- DATA exports use `dataset_evidence_only_not_motion_permission` and include expected classes `water_bottle`, `end_effector`, and `gripper_tip`, plus source/frame/semantic/EMG/simulation/safety evidence. This remains data/annotation evidence only, not a robot command path.
- Deployed the DATA module to the cloud. Local and cloud `npm --workspace apps/web run build` passed, with only pre-existing hook warnings. Cloud QA confirmed `/rehab-stitch/data.html`, `2/2` stereo frames, evidence JSON download, `V / 视觉` transition, and no horizontal overflow at 1600 px. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/data-hub-20260703/`.
- Refined the DATA page workbench bridge into four explicit actions: `采集 -> robotics?tab=camera`, `标注 -> robotics?tab=dataset`, `训练 -> robotics?tab=model`, and `评估 -> robotics?tab=chart`, all scoped to the selected device. The exported DATA evidence now includes these `workbench_routes`.
- Cloud QA passed after deployment: all four actions appeared in `/rehab-stitch/data.html`, the exported JSON contained the expected routes, and clicking `评估` navigated to `/robotics?tab=chart&device=nanopi-m5`. Evidence is under `docs/screenshots/rehab-arm-stitch-first-qa/data-hub-actions-20260703/`.
- Tightened the L/DATA contract: `data_collection` semantic routing now opens the same-level `DATA / 数据` module instead of returning to `V / 视觉`, and the L sidebar database icon routes to `data_hub`. Local build passed; static router QA confirmed one `data_hub` data-capture entry and zero legacy `vision` data-capture entries. Evidence: `docs/screenshots/rehab-arm-stitch-first-qa/data-hub-actions-20260703/local-router-static-1600.png`.
- Extended the NanoPi read-only upload path for two USB RGB cameras. `scripts/nanopi-rehab-arm-readonly-agent.py` now accepts `--left-keyframe-file` and `--right-keyframe-file`, uploads them as `stereo_left`/`stereo_right` keyframes, and posts a `stereo_rgb_yolo_context_v1` record with waiting detector/calibration state.
- `scripts/nanopi-rehab-arm-collect-and-upload.py` can now either forward existing left/right frame files or capture one frame from each USB camera with OpenCV via `--capture-stereo-keyframes --left-camera-index ... --right-camera-index ...`. This remains read-only V evidence and does not touch CAN, ROS writes, M33, or motor control.
- Validation passed: `python -m pytest scripts/test_nanopi_rehab_arm_readonly_agent.py scripts/test_nanopi_rehab_arm_collect_and_upload.py -q` (`6 passed`). NanoPi stereo usage was documented in `docs/rehab-arm-nanopi-vla-mujoco-integration.md`.
- Generalized `/me.home_status_guide.secondary_actions` beyond offline evidence. The first phone card now derives deduped secondary actions from the daily action's source guide for active-session recovery, finished-session report generation, report follow-up, accepted AI plan sync/preflight, safety review, and offline evidence handling.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). This remains App HTTP workflow/evidence guidance and does not create motor/CAN/M33 override authority.
- Added `/me.ai_draft_review_guide` for open AI training drafts. It returns `VIEW_AI_DRAFT` and `ACCEPT_AI_DRAFT` actions and feeds the home card secondary actions, so reviewing a draft and accepting it into a normal plan no longer requires frontend endpoint inference.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). Accepting a draft still only creates a plan; M33 sync/acceptance, preflight, and readiness gates remain required before session start.
- Added explicit `actions` to `/me.onboarding_guide` and `/me.primary_start_guide`. First-run setup now exposes the remaining profile/device/plan actions, and training readiness now exposes `VIEW_START_GUIDE`, `CHECK_START_READINESS`, and the current blocker/start action so the home card can guide setup and readiness without frontend inference.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). These actions remain workflow guidance only and do not grant hardware motion authority.
- Added backend-authored `blocker_details` to `/me.care_summary` and `/me.home_status_guide`. Blockers now include stable code plus user-facing title/description for onboarding, active session, report review, AI draft, failed offline evidence, and pending offline sync states.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). This is display guidance only and does not alter safety gates or hardware authority.
- Extended `blocker_details` with `severity` and `related_action_codes` so phone UI can style blockers and link them to the current primary/secondary actions without maintaining its own blocker-to-action map.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only).
- Added `/me.home_status_guide.action_groups` with `primary`, `secondary`, and `blocker_related` action groups. The backend now groups actions for the current blocker, so the phone can render "why blocked" and "what to tap" from one payload.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only).
- Added `start_readiness_blocked` to `/me.care_summary.blockers` for the state where onboarding is complete but the plan/device still cannot start because M33 acceptance, preflight, safety review, or another readiness check is pending.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only).
- Added `/me.home_status_guide.progress.next_item_actions` so the phone home card can render CTAs for the highlighted progress item directly from backend-authored action matching.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). This remains App HTTP workflow/evidence guidance and does not create BLE, CAN, motor, or M33 override authority.
- Added `/me.home_status_guide.progress.next_item_blockers` so the phone home card can render the highlighted progress row's blocker severity, explanation, and clear condition without locally matching blocker codes.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). The field mirrors existing blocker details and remains UI workflow guidance only.
- Added `/me.home_status_guide.progress.next_item_context`, a backend-composed slice containing the highlighted progress item, matched actions, matched blockers, counts, and an evidence-only control boundary.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). This gives phone/Stitch shells a direct next-step card payload without granting motion authority.
- Added `primary_action` and `secondary_actions` inside `/me.home_status_guide.progress.next_item_context` so the phone can render the highlighted progress row's main CTA and follow-up CTAs without locally re-prioritizing actions.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). Failed offline evidence keeps failed-review and queued-replay secondary actions when both states coexist.
- Added `display` copy to `/me.home_status_guide.progress.next_item_context` with title, description, tone, severity, and clear condition derived from the matched progress item/blocker.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). This keeps the phone next-step card copy backend-authored and evidence-only.
- Added `completion_percent`, `completion_label`, and `remaining_label` to `/me.home_status_guide.progress` so phone shells can render workflow progress without recalculating checklist totals.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). The percentage covers the full home workflow checklist, not just onboarding substeps.
- Added `next_item_position` and `next_item_label` to `/me.home_status_guide.progress` so phone step indicators can show the backend-highlighted progress row without scanning checklist order.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). Position is 1-based and becomes null when the home workflow is complete.
- Added backend-authored `status`, `status_label`, and `tone` to each `/me.home_status_guide.progress.items` row so phone checklist rendering can distinguish done/current/pending rows without local inference.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). Item tone inherits blocker severity for the current row and uses success for completed rows.
- Added per-row `position` and `position_label` to `/me.home_status_guide.progress.items` so phone checklist rows can render their own step number without reading array indexes.
- Validation passed with `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`7 passed`, deprecation warnings only). Row positions mirror the backend checklist order used by `next_item_position`.
- Synced the rehab-arm mobile backend closure to the cloud server after the branch had only been pushed to GitHub. Cloud `/api/health` and the Web proxy now report `build_sha=17d7e78b2b10`, `build_ref=app/rehab-arm-mobile-stitch`.
- Cloud validation passed: server-side `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` returned `7 passed`, `scripts/check_web_api_alignment.py --web-base http://106.55.62.122:3001 --api-base http://106.55.62.122:8011 --project-id e201f41c-25a6-46e1-baf8-be6dcb83284c` returned `ok: true`, and real login `3245056131@qq.com` reached `/api/rehab-arm/app/v1/me` with backend-authored home progress fields and evidence-only safety boundaries.
- Extended the numerical IK evidence loop with explainable solver fields. `ik_solver_report` now includes target position, residual vector, seed count, precise/approximate thresholds, status reason, and a joint-boundary summary showing nearest/near-limit joints.
- Updated the Stitch-bound `3D / 孪生` IK panel to show solver reason and near-limit count in addition to quality/error, keeping the same evidence-only export boundary.
- Enhanced `scripts/validate-rehab-arm-ik-loop-cloud.mjs` so `qa-metrics.json` records solver quality, position error, residual vector, seed count, status reason, and near-limit joint count.
- Enhanced `scripts/validate-rehab-arm-mujoco-shadow-candidate.py` to parse `/sim/medical_arm/joint_states` echo output into structured `observed_joint_state`, `target_joint_positions`, and `tracking_error_rad` evidence.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_sync.py -q` (`43 passed`), `npx --workspace apps/web tsc --noEmit`, `npm --workspace apps/web run build`, and `python -m py_compile scripts/validate-rehab-arm-mujoco-shadow-candidate.py`.
- Deployed the explainable IK slice to the cloud with scoped file upload, `npm run build:web`, and `RESTART=1 bash scripts/start-cloud-prod.sh`; API `:8011` and Web `:3001` health checks passed.
- Cloud QA evidence refreshed under `docs/screenshots/rehab-arm-stitch-first-qa/ik-loop-20260703-explain-v3/`. The browser QA generated `candidate_approximate`, `solverPositionErrorM=0.1731`, `solverSeedCount=13`, `nearLimitJointCount=5`, evidence-only boundaries, and `dangerousMatches=[]`.
- MuJoCo shadow execution evidence for the same exported candidate was written to `docs/screenshots/rehab-arm-stitch-first-qa/ik-loop-20260703-explain-v3/mujoco-shadow-execution-evidence.json`. It published only `/sim/medical_arm/joint_trajectory`, observed `/sim/medical_arm/joint_states`, and reported structured tracking error with max absolute joint error about `0.4333 rad` on the sim-only path.

## 2026-07-04

- Rebuilt the rehab-arm control-room `L / 模式` and `A / 动作` modules as Stitch-first pages (`apps/web/public/rehab-stitch/router.html` and `planner.html`) and bound the existing React iframe bridge to their data-role contract instead of pasting old dashboard cards over the new design.
- `L / 模式` now shows the XiaoZhi/L transcript summary, semantic mode, route confidence, target, shared mode resource states, and dispatch evidence JSON while keeping the XiaoZhi WebSocket/ASR/TTS/M55 semantic chain untouched. Its action buttons route to the existing vision, A planner, training, EMG, and logs modules.
- `A / 动作` now shows L mode, V target/end-effector evidence, depth/calibration state, dry-run gate, MuJoCo shadow state, M33 final-authority state, editable target XYZ inputs, IK quality/residual/seed/near-limit evidence, and export-only candidate JSON. It remains a dry-run/evidence surface and does not expose CAN frames, raw motor commands, torque/current/raw position/velocity, M33 override, or emergency-stop release.
- Validation passed locally: `npm --workspace apps/web run build` succeeded. Browser QA used the production preview at `http://127.0.0.1:3108/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control`, entered the Stitch iframe, clicked `L` and `A`, and captured evidence under `docs/screenshots/rehab-arm-la-stitch-qa/`. The A-page target input test kept `X=0.456` and `Y=-0.123` after iframe refresh delay, confirming the planner inputs are no longer immediately overwritten.
- Deployed the scoped L/A Stitch rebuild to Tencent cloud by uploading only `router.html`, `planner.html`, the rehab control React bridge, and docs, then running cloud `npm run build:web` plus `RESTART=1 bash scripts/start-cloud-prod.sh`. Cloud QA on `http://106.55.62.122:3001/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control` clicked the iframe `L` and `A` modules, confirmed `router-transcript` showed live XiaoZhi/L text, confirmed A/IK/M33/MuJoCo evidence rendered, and kept planner `X=0.456` after refresh delay. Screenshots: `docs/screenshots/rehab-arm-la-stitch-qa/cloud-mode-router-1600.png` and `cloud-action-planner-1600.png`.
- Known UI follow-up: desktop L/A pages are usable and no old cards cover the Stitch surface; the narrow 390px control-shell top status still compresses `等待设备 / LIVE / READ ONLY / 急停`, which should be handled as a global shell responsive pass rather than a one-off L/A patch.
- Reworked the `A / 动作` page again with Stitch into a more vivid action-planning stage. The new `planner.html` keeps the Stitch design as the primary surface and adds a central `SIMULATION_POD` with a glowing end-effector-to-target trajectory, target reticle, particle/scanline motion, bottom pipeline (`V_LOCK -> IK_SOLVE -> SIMULATE -> M33_GATE`), and a visible dry-run/M33 safety curtain so judges can see "action is being planned, but not executed".
- Preserved the existing A-page function contract by keeping data roles for L/V evidence, end-effector/depth state, editable target XYZ inputs, IK generate/export, MuJoCo/M33/dry-run state, IK quality/residual/seeds/near-limit, and candidate JSON. Local QA confirmed the platform sidebar enters `/rehab-stitch/planner.html`, `SIMULATION_POD` renders inside the control iframe, React injects the live mode/gate/evidence text, and manually typed `X=0.456`, `Y=-0.123` remain after refresh delay. Evidence: `docs/screenshots/rehab-arm-action-vivid-qa/vivid-a-platform-refreshed-1600.png` and `vivid-a-platform-input-persist-1600.png`.
- Deployed the vivid A-page stage to Tencent cloud with a scoped `planner.html` upload, cloud `npm run build:web`, and `RESTART=1 bash scripts/start-cloud-prod.sh`. Cloud QA clicked the real control-room sidebar `A / 动作`, confirmed `/rehab-stitch/planner.html`, `SIMULATION_POD`, M33 gate, live planner roles, and candidate JSON all render on the public URL. Evidence: `docs/screenshots/rehab-arm-action-vivid-qa/cloud-vivid-a-1600.png`.
- Fixed AI model relay token copy failure on the rehab-arm control room. `apps/web/app/projects/[id]/rehab-arm-control/rehab-arm-control-client.tsx` now uses a clipboard helper that tries `navigator.clipboard.writeText` in secure contexts and falls back to a temporary textarea copy path for cloud/iframe/browser-permission cases. The same helper was added to the untracked `model-relay-lab` client for its edge package copy button.
- Validation passed: `npm run lint` from `apps/web` completed successfully with only pre-existing React hook warnings in `project-playable-shell.tsx` and `rehab-arm-control-client.tsx`.
- Deployed the AI model relay token copy fix to the Tencent cloud runtime by copying the scoped control-room client and docs, running cloud `npm run build:web`, and restarting with `RESTART=1 scripts/start-cloud-prod.sh`. Cloud Web/API health passed with `build_time=2026-07-04T15:10:42Z`; the production rehab-arm control chunk contains the textarea/`execCommand("copy")` clipboard fallback.
- Extended the Stitch Bluetooth debug page with a direct backend trusted-device binding action. After Android reads paired SPP devices, the App can POST the selected MAC/name to `/api/rehab-arm/app/v1/devices/bind` with `trust_status=trusted`, then continue through plan sync and backend-approved legacy frame send from the same validation page.
- Published local Android debug APK `1.0.9` (`versionCode=10`) to `apps/web/public/downloads/rehab-arm/lingdong-rehab-arm-debug.apk`. APK metadata: SHA256 `155EEBB6B4FA1C001DD82744B3F4A92356B757784EBC3778444F755AA2C020BF`, size `4,189,022 bytes`.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`11 passed`), `node --check` for PWA and Android `mobile-bridge.js`, `npm run build:debug`, `aapt dump badging` showed `versionCode=10 versionName=1.0.9`, `apksigner verify --print-certs` passed, and APK assets include `bluetooth-debug.html` / `device.html` / `mobile-bridge.js`.
- Browser QA captured `docs/screenshots/rehab-arm-bluetooth-debug-bind-qa/bluetooth-debug-bind-mobile.png`; Web/PWA mode still correctly shows `仅 Web/无权限` and now exposes `后端绑定：等待登录` before Android-native pairing is available. Physical Android/M33 ACK validation remains pending.
- Added a Stitch-designed mobile `bluetooth-debug.html` page for "蓝牙调试 / M33 实机验证" and linked it from the App device page. The page lists Android paired SPP devices, connects/disconnects the native SPP bridge, previews only backend-generated `legacy_transport_frame.wire_text`, sends only `sendable=true` frames, and displays TX/RX/API/sensor evidence.
- Reused the old verified App Bluetooth contract: Bluetooth Classic SPP/RFCOMM UUID `00001101-0000-1000-8000-00805F9B34FB` with newline-delimited UTF-8 JSON. The page intentionally excludes raw CAN, motor current/torque/position/velocity, M33 override, and emergency-stop release controls.
- Published local Android debug APK `1.0.8` (`versionCode=9`) to `apps/web/public/downloads/rehab-arm/lingdong-rehab-arm-debug.apk`. APK metadata: SHA256 `772B5D618CA4553C25E2A10B6F79254DDE968D143B3E1BE1F8F21D6604D3705C`, size `4,188,454 bytes`.
- Updated `/api/rehab-arm/app/v1/public-config` download metadata and the download page to describe the Stitch Bluetooth debug console. Validation passed locally: `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`11 passed`), `node --check` for PWA and Android `mobile-bridge.js`, `npm run build:debug`, APK asset inspection confirmed `assets/public/bluetooth-debug.html`, `aapt dump badging` showed `versionCode=9 versionName=1.0.8`, and `apksigner verify --print-certs` passed.
- Browser QA served the mobile PWA locally and captured `docs/screenshots/rehab-arm-bluetooth-debug-qa/bluetooth-debug-mobile.png`, `device-entry-mobile.png`, and `device-entry-full-mobile.png`. In Web/PWA mode the debug page correctly shows `仅 Web/无权限` instead of fake SPP connected state; physical Android/M33 ACK validation remains pending until hardware protocol/firmware confirmation.
- Added backend-renderable `payload_schema` and `form_contract` to rehab App `/me/workflow.action_queue` entries, and exposed `frontend_contract.workflow_action_endpoint` plus the rule that phone shells should render action forms from backend schema instead of hard-coding payloads.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`11 passed`, deprecation warnings only). The schema contract is workflow/evidence guidance only and does not grant BLE, CAN, M33, motor, or emergency-stop authority.
- Deployed preflight workflow action commit `4609ebf2` to Tencent cloud. Cloud backend tests passed with `11 passed`; a temporary cloud user completed setup, plan sync, real M33-status evidence, and preflight through `/me/workflow/actions`, reached `ready_to_start`, and the App HTTP boundary remained evidence-only.
- NanoPi VLA-V target recognition was switched from weak `yolo11n.onnx`/`0.20` evidence to `yolo11s.onnx` with `REHAB_TARGET_CONF=0.45`. Live stereo evidence on `nanopi-m5` showed real Baisui Mountain bottle detections, with right-camera confidence repeatedly around `0.79-0.85` and left-camera confidence around `0.55-0.76`; only one `nanopi_vla_cpp_upload_loop.py` process was left running.
- Investigated missing end-effector recognition. The deployed `/home/pi/rehab_vla/best.onnx` produced no accepted detections at `0.20`; raw ONNX scores on the live frame were only about `0.045` for `end_effector` and `0.076` for `gripper_tip`. Local `best.pt` on the same frame reached only about `0.157`, and exported ONNX remained around `0.032`, so the issue is primarily poor generalization to the current far/partially-occluded fixed-camera scene, with ONNX adding extra degradation. Next step: add current left/right hard examples to the existing `D:\vla_dataset\20260627_213112\yolo_end_effector_v1_with_negatives` dataset and retrain/export a current-camera end-effector model.
- Added `POST /api/rehab-arm/app/v1/me/workflow/actions` as the App's unified safe workflow-action endpoint. It executes only backend-available, whitelisted workflow actions such as `READY_TO_START`, `FINISH_SESSION`, `GENERATE_TRAINING_REPORT`, `RECORD_REPORT_REVIEW`, `DRAFT_NEXT_PLAN_FROM_REPORT`, `ACCEPT_AI_DRAFT`, offline replay/review, and safety-review evidence.
- Preserved the safety boundary: `M33_ACCEPTANCE_REQUIRED`, `RECORD_M33_DECISION`, direct motor commands, CAN frame send, M33 override, emergency-stop release, and App-granted motion permission are explicitly rejected with `WORKFLOW_ACTION_FORBIDDEN`.
- Updated `public-config` with `workflow_action_endpoint`, extended `mobile_boot_flow`, and published Android debug APK `1.0.4` (`versionCode=5`) with a workflow panel button that calls the unified endpoint and refreshes backend workflow state.
- APK `1.0.4` download metadata: `DFCBD3EADEE230947A0E6FC5AFCADAD46095CCC5963A9FE9ACDF7EBC355031B2`, `4,173,198 bytes`, package `com.lingdong.rehabarm`.
- Validated locally: `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`10 passed`), `node --check` for both PWA and Android `mobile-bridge.js`, `npm run build:debug`, `apksigner verify --verbose`, and `aapt dump badging` showing `versionCode=5 versionName=1.0.4`.
- Next step: deploy the branch to cloud, verify `/public-config` reports `1.0.4`, download the cloud APK, and QA the phone/PWA workflow-action button with the test account.
- Added `/me.daily_care_plan` as a backend-authored phone home plan. It composes the existing home progress checklist, primary task, current action, blocker details, counts, and a three-item timeline preview into `rehab_app_daily_care_plan_v1` so Stitch/mobile shells can render the user's day without rebuilding state from scattered guide fields.
- Updated `public-config.rehab_app.daily_care_plan_path` to `data.daily_care_plan` and included `daily_care_plan` in `/me/workflow.guides` for QA/debug visibility. The field is explicitly evidence/workflow guidance only and does not grant motion permission.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` from `apps/api` (`10 passed`, deprecation warnings only). Tests now cover the start-readiness-blocked state and the ready-to-start state for the daily care plan.
- Current APK remains `1.0.5` (`versionCode=6`, SHA256 `D29649F38FA7684ED87EE9F42139DFED3C93FC38C6D8933ABC62650285DBF45C`); this backend-only slice does not rebuild the binary.
- Fixed the rehab-arm control room `3D / 孪生` IK coordinate panel so operator-entered candidate coordinates no longer reset to defaults during Stitch iframe/dashboard refresh. The panel now preserves a draft for `target_robot_frame`, approach vector, gripper orientation, and source, and IK generation/export reads the latest draft immediately.
- Synchronized platform robot-render ordering with the MuJoCo 6DOF shadow chain. New platform-generated `robot_render_state_v1` emits `jian_hengxiang_joint -> jian_zongxiang_joint -> jian_xuanzhuan_joint -> zhou_zongxiang_joint -> wanbu_zongxiang_joint -> wanbu_hengxiang_joint`, while still mapping motor IDs through the existing `ARM_JOINT_MAP`.
- Updated the imported URDF render path to preserve the model's metric root coordinates/scale for MuJoCo-shadow comparison; the browser camera now frames the imported URDF instead of translating and normalizing the model geometry.
- Validation passed locally: `python -m pytest tests/test_rehab_arm_sync.py -q` from `apps/api` (`43 passed`) and `npm --workspace apps/web run build` (success with pre-existing React hook warnings). Deployed scoped frontend/backend file updates to Tencent cloud and restarted API/Web.
- Cloud QA passed on `http://106.55.62.122:3001/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control`: typed IK values `0.41/-0.13/0.29` plus approach `0,1,-1` remained after `3D / 孪生` refresh. Evidence is under `docs/screenshots/rehab-arm-twin-ik-reset-qa/`; cloud API confirmed `nanopi-m5.robot_render_state.joint_names` in MuJoCo order.

## 2026-07-05

- Added the backend `vla_closed_loop_status` contract to rehab-arm dashboard devices. The new `rehab_arm_vla_closed_loop_status_v1` summary fuses the current L semantic mode, V target/end-effector/robot-frame readiness, A/IK candidate status, MuJoCo shadow readiness, and M33 safety gate into a five-stage `L -> V -> A -> SIM -> M33` evidence pipeline with explicit `action_state`, `next_step`, and blockers.
- Bound the A/动作 Stitch iframe bridge to the new closed-loop status without changing the Stitch visual design. The planner candidate JSON now includes `vla_closed_loop_status`, and the A loop-state labels/reason text prefer backend blockers such as `camera_to_robot_transform_missing` over frontend guesses. This remains `vla_closed_loop_status_only_not_motion_permission` and does not change XiaoZhi/L transport, V upload, IK generation authority, MuJoCo publish targets, M33, CAN, or real motion paths.
- Validation passed: `python -m pytest apps/api/tests/test_rehab_arm_vla_closed_loop_status.py -q` (`1 passed`), `python -m pytest apps/api/tests/test_rehab_arm_sync.py -q` (`44 passed`), `npx --workspace apps/web tsc --noEmit`, and `npm --workspace apps/web run build` (success; only pre-existing React hook warnings remain for `project-playable-shell.tsx` and `requestModelRelay`).
- Added a backward-compatible V visibility fallback so `vla_closed_loop_status` can infer target/end-effector readiness from legacy `detections` labels such as `water_bottle` and `gripper_tip` when an older schema drops `target_object` or `end_effector_object`.
- Deployed the closed-loop status slice to Tencent cloud with `AI_COLLAB_BUILD_SHA=rehab-vla-closed-loop-status-20260705`. Cloud health passed, `test_rehab_arm_vla_closed_loop_status.py` passed on the server, and the live dashboard response includes `vla_closed_loop_status`. Cloud authenticated A-page QA confirmed `/rehab-stitch/planner.html` shows the backend status in candidate JSON; evidence: `docs/screenshots/rehab-arm-vla-closed-loop-qa/cloud-action-closed-loop-1600.png` and `cloud-action-closed-loop-qa.json`. Cloud `test_rehab_arm_sync.py` still has one unrelated XiaoZhi opus expectation failure (`asr_not_configured` vs `opus_*`) in the server environment; L transport was not modified by this slice.
- Added a Windows local fixed-camera end-effector dataset capture helper at `scripts/local-rehab-arm-end-effector-capture.py`. It provides real-time OpenCV preview, Space-to-save capture, `Q`/`Esc` exit, stereo or single-camera mode, and camera flip options for the current fixed rig.
- The new default capture dataset is `D:\vla_dataset\20260705_fixed_camera_end_effector\images\train`, with matching YOLO labels expected under `D:\vla_dataset\20260705_fixed_camera_end_effector\labels\train`; both directories were created locally. Classes stay `end_effector` and `gripper_tip`.
- Validation passed: `python -m py_compile scripts/local-rehab-arm-end-effector-capture.py` and `python scripts/local-rehab-arm-end-effector-capture.py --help`. This is local dataset capture only and does not touch NanoPi upload, XiaoZhi/L, platform control, CAN, M33, or motion paths.
- Captured a NanoPi fixed-camera end-effector hard-example session over SSH without stopping the live VLA-V upload loop. `nanopi-m5` resolved to `192.168.3.36`; logical stereo sources stayed `/dev/video45` and `/dev/video47` with `hv` flip. The session saved 12 synchronized stereo pairs under `/home/pi/rehab_arm_fixed_end_effector_capture/fixed_end_effector_20260704_140434`, copied them back to `D:\vla_dataset\20260705_fixed_camera_end_effector\nanopi_sessions\fixed_end_effector_20260704_140434`, and flattened 24 annotatable images into `D:\vla_dataset\20260705_fixed_camera_end_effector\images\train` with the prefix `nanopi_20260704_140434_`.
- Captured the requested 1000 stereo-pair NanoPi hard-example dataset for the current fixed end-effector scene. The session `fixed_end_effector_bulk_20260705_030943` saved 1000 left frames and 1000 right frames on NanoPi under `/home/pi/rehab_arm_fixed_end_effector_capture/fixed_end_effector_bulk_20260705_030943`, pulled them back to `D:\vla_dataset\20260705_fixed_camera_end_effector\nanopi_sessions\fixed_end_effector_bulk_20260705_030943`, and flattened 2000 annotatable images into `D:\vla_dataset\20260705_fixed_camera_end_effector\images\train`.
- During high-volume capture, the live VLA-V upload loop was stopped to avoid `/dev/video45` and `/dev/video47` contention, each batch uploaded its latest stereo pair to the platform, and the live loop was restarted afterward. Final QA counted 1000 local stereo pairs, 1000 remote left frames, 1000 remote right frames, generated `bulk_sample_contact_sheet.jpg`, and reduced the NanoPi live upload loop back to one process.
- Continued mobile App user-view QA for the training-library/calendar loop. The PWA/Android bridge now renders backend training plans into the existing Stitch training-library cards, including title, target angle, assist level, persisted status, and the correct next step to the device sync page. Empty plan cards now route to AI planning instead of showing static demo exercise data.
- Fixed the shared mobile history panel guard. `真实康复历史` now appears on the training-library page as backend evidence, while device and AI planner pages stay focused on their local actions. The profile calendar still owns the richer day-by-day history view.
- Deployed the scoped `mobile-bridge.js` update to the Tencent cloud runtime with `AI_COLLAB_BUILD_SHA=mobile-training-loop-r2-20260705`.
- Validation passed: `node --check apps/web/public/rehab-arm-mobile/mobile-bridge.js`, `node --check apps/mobile/rehab-arm-android/www/mobile-bridge.js`, and `python -m pytest apps/api/tests/test_rehab_arm_app_backend.py -q -o faulthandler_timeout=60` (`13 passed`, deprecation warnings only).
- Cloud mobile QA passed on `http://106.55.62.122:3001/rehab-arm-mobile`: authenticated training-library/device/AI pages had zero horizontal overflow, zero HTTP 4xx/5xx responses, the first real plan card navigated to `device.html`, `一键同步训练计划` still explained missing trusted M33/PSoC binding, and the profile calendar showed persisted AI draft/plan activity. Evidence is under `docs/screenshots/rehab-arm-mobile-user-qa-20260705-cloud-r4-final/`.
- Published Android debug APK `1.0.10` (`versionCode=11`) to `apps/web/public/downloads/rehab-arm/lingdong-rehab-arm-debug.apk` so phone installs include the training-library backend-plan binding. APK metadata: SHA256 `DB674D09F46D0CF0B8DE9717D1904A52CE042BC19E49FB1BEB45BC4548E9BB52`, size `4,198,462 bytes`.
- Validation passed for the package: `npm run build:debug`, `aapt dump badging` showed `versionCode=11 versionName=1.0.10`, `apksigner verify --print-certs` passed, and package assets include `assets/public/mobile-bridge.js` with `renderTrainingLibraryPage`, `assets/public/training-library.html`, and `assets/public/bluetooth-debug.html`.
- Cloud download-page QA passed at 390 px mobile width: `下载 APK` produced `lingdong-rehab-arm-debug.apk` with `4,198,462` bytes, the page showed `1.0.10` plus the matching SHA256, had zero horizontal overflow, and no HTTP 4xx/5xx or console errors. Evidence is under `docs/screenshots/rehab-arm-apk-qa-20260705-1010/`.
- Converted the user-labeled fixed-camera `gripper` dataset into a single-class YOLO split and trained a YOLO11n gripper model. Dataset root is `D:\vla_dataset\20260705_fixed_camera_end_effector`; split root is `D:\vla_dataset\20260705_fixed_camera_end_effector\yolo_gripper_v1`; run output is `D:\vla_dataset\20260705_fixed_camera_end_effector\runs\gripper_yolo11n_416_e15`. Validation was strong on the fixed-camera split: PyTorch precision/recall/mAP50 about `0.994`, ONNX precision/recall/mAP50 about `0.994`, with prediction QA contact sheet at `D:\vla_dataset\20260705_fixed_camera_end_effector\meta\gripper_predict_contact_sheet.jpg`.
- Deployed the new ONNX to NanoPi as `/home/pi/rehab_vla/gripper_yolo11n_416_e15.onnx` and patched `/home/pi/rehab_vla/nanopi_vla_ort_infer.py` to correctly parse both old two-class `1x6xN` outputs and new single-class `1x5xN` outputs. The single `gripper` class is normalized to downstream `gripper_tip` evidence via `REHAB_END_EFFECTOR_SINGLE_CLASS_LABEL=gripper_tip`.
- Updated `/home/pi/rehab_vla/rehab_vla_vision.env` so `rehab-vla-vision.service` auto-loads the new gripper model on boot: `REHAB_END_EFFECTOR_ONNX=/home/pi/rehab_vla/gripper_yolo11n_416_e15.onnx`, `REHAB_END_EFFECTOR_CONF=0.45`, and `REHAB_END_EFFECTOR_SINGLE_CLASS_LABEL=gripper_tip`. The target detector remains `/home/pi/rehab_arm_models/yolo/yolo11s.onnx` at `REHAB_TARGET_CONF=0.45`.
- NanoPi live validation: `rehab-vla-vision.service` is active/enabled with exactly one `nanopi_vla_cpp_upload_loop.py` process, logical cameras `/dev/video45` and `/dev/video47`, both `hv` flipped. Current live frames were pulled to `docs/screenshots/rehab-arm-gripper-live-qa/latest_left_after_gripper_model.jpg` and `latest_right_after_gripper_model.jpg`; the gripper is at the right edge/partially occluded, and the model correctly reports no accepted gripper detection at `0.45` (`score max` around `0.0766`). Platform context remains evidence-only with `pixel_servo_hint.next_step=hold_observe`.
- Rechecked the NanoPi deployment using 12 annotated validation originals copied back to NanoPi. The same deployed `/home/pi/rehab_vla/gripper_yolo11n_416_e15.onnx` and parser produced detections on all 12 images with confidence `0.713` to `0.796`, proving the deployed model/parser/preprocessing path works on known positives. The live scene still gives only weak/false gripper responses, so the remaining blocker is current scene distribution/framing/occlusion rather than a wrong ONNX upload.
- Refreshed the rehab-arm `L / 模式` and `A / 动作` Stitch pages again for stronger VLA demo feel. `router.html` now foregrounds semantic parsing, intent/entity evidence, mode classification, shared-mode dispatch, and the safety boundary that L only emits semantic intent. `planner.html` now foregrounds L+V fusion into A, action preview, dry-run/M33 lock, MuJoCo/IK state, editable target XYZ, IK evidence, and candidate JSON without exposing raw motion authority.
- Preserved the existing iframe data-role contract for L/A (`router-*` and `planner-*` roles), added the missing router mode/training/muscle hooks, kept planner XYZ editable, and added responsive fallback CSS for the action planner's narrow viewport. Validation passed with `npm --workspace apps/web run build`; static Chrome QA screenshots were captured under `docs/screenshots/rehab-arm-la-stitch-qa-20260705/`.
- Local platform-route QA note: the existing `127.0.0.1:3000` Next dev process was stale and displayed a missing `app/not-found.module.css` build overlay for the outer platform route, while the static Stitch pages and production build were healthy. Restart the local web server before using the outer `/projects/.../rehab-arm-control` page for another iframe QA pass.
- Deployed the scoped L/A Stitch refresh to Tencent cloud by uploading only `apps/web/public/rehab-stitch/router.html` and `planner.html`, then running cloud `npm run build:web` and `AI_COLLAB_BUILD_SHA=rehab-la-stitch-vla-20260705 RESTART=1 bash scripts/start-cloud-prod.sh`. Cloud Web/API health passed and `/api/proxy/health` reports `build_sha=rehab-la-stitch-vla-20260705`.
- Cloud static QA confirmed the public L/A pages contain the new Chinese VLA semantic/action copy (`VLA-LITE 语义调度`, `灵动康复机械臂`, `动作预演与融合推理`) and captured `docs/screenshots/rehab-arm-la-stitch-qa-20260705/cloud-router-static-1600.png` plus `cloud-planner-static-1600.png`. Full authenticated iframe click QA remains a follow-up because the generic CDP screenshot helper checks the outer React document and does not enter the Stitch iframe.
- Completed the follow-up authenticated iframe QA and deployed the shell fix with `AI_COLLAB_BUILD_SHA=rehab-la-iframe-shell-fix-20260705`. The rehab control bridge now hides the Stitch page's own `nav`/`aside` inside embedded `body > div.flex`, so cloud `L / 模式` and `A / 动作` show the unified platform sidebar only instead of a double sidebar. Real cloud QA logged in as the test account, clicked the iframe L/A modules, confirmed `/rehab-stitch/router.html` and `/rehab-stitch/planner.html`, confirmed nested nav/aside were hidden, and confirmed planner XYZ inputs are editable. Evidence: `docs/screenshots/rehab-arm-la-stitch-qa-20260705/cloud-platform-mode-router-iframe-shellfix-1600.png`, `cloud-platform-action-planner-iframe-shellfix-1600.png`, and `cloud-platform-iframe-shellfix-qa.json`.
