# Codex 后端与背包协作推进补充 - 2026-04-21 Git 工作流

## 本轮目标
- 把 `Git 合作` 页从说明堆叠收成更像 AI 维护员工作台。
- 继续按“用户也是开发者”的视角推进，不只展示，要能从待分流任务快速创建 AI / 人工需求单。
- 继续验证前端构建和后端测试，并检查真实页面截图。

## 本轮改动

### 1. Git 合作页增加维护员总览
文件：
- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.tsx`

新增：
- `requirementSummary`
- Git 页顶部总览卡，展示：
  - 待分流需求
  - AI 需求单
  - 人工需求单
- 站内锚点入口：
  - `Git 配置`
  - `分给 AI`
  - `分给人`
  - `版本活动`
  - `Git 边界`

目的：
- 不再让 Git 页只是长滚动列表。
- 打开后先让用户知道“这里是维护员分流工作台”，再决定去哪一块。

### 2. 待分流任务支持一键快速分派
文件：
- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.tsx`

新增：
- 对 `aiRouteCandidates` 增加 `快速分给首个 AI`
- 对 `humanRouteCandidates` 增加 `快速分给首位协作者`

实现方式：
- 直接调用已有 `createProjectRequirement`
- 自动补最小字段：
  - `project_id`
  - `requirement_type`
  - `status`
  - `priority`
  - `from_agent`
  - `to_agent`
  - `title`
  - `module`
  - `context_summary`
  - `expected_output`
  - `opening_message`

效果：
- Git 维护员现在可以从待分流任务里直接点一下，生成对应的 AI / 人工需求单。

### 3. 背包打开时隐藏 HUD 与快捷栏
文件：
- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.tsx`
- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.module.css`

新增：
- `overlayHidden`
- `inlineStack`

行为变化：
- 右侧背包面板打开时：
  - 左上 HUD 隐藏
  - 右下快捷栏隐藏

目的：
- 避免继续压住右侧面板。
- 让背包页在“打开时像单独界面”，收起后再回到地图操作。

## 验证

### 构建
- `npm run build:web` 通过

### 后端
- `python -m pytest tests -q` 通过
- 结果：`72 passed`

### 页面截图
- `D:\ai合作产品\artifacts\team-git-workflow-verify-2026-04-21.png`
- `D:\ai合作产品\artifacts\team-git-workflow-verify-2026-04-21-b.png`

截图确认：
- Git 合作页顶部已经出现维护员总览卡
- 可以看到 `Git 配置 / 分给 AI / 分给人 / 版本活动 / Git 边界`
- 面板打开时，右下快捷栏不再继续压住右侧面板

## 当前结论
- Git 合作页已经开始像“维护员分流工作台”，不是单纯说明页。
- 需求分给 AI / 分给人的最小工作流已经有可点击入口。
- 背包打开时的前端压迫感收了一层。

## 下一步建议
- 继续做“AI / 人工需求的回执和状态变化”，让 Git 分流不只停在创建需求单。
- 把 Git 页里的回执再压成更明确的最小报告流。
- 继续从用户视角走一遍“看到待分流任务 -> 分给 AI -> 在信息交流里看到回执”的完整链路。

## 本轮补充 - 需求回执串联

### 改动
- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.tsx`
  - 新增 `isRequirementLinkedMessage()`
  - 新增 `requirementMessageMap`
  - 新增 `requirementFlowFeed`
  - `Git 合作 -> 分给 AI / 分给人` 的需求卡片里，都会显示这条需求的“最近回执”
  - `信息交流` 页新增 `按需求看回执` 卡片，把需求状态和最近一条回执串在一起看

### 验证
- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`
- 新截图：
  - `D:\ai合作产品\artifacts\team-exchange-requirement-flow-verify-2026-04-21-b.png`

### 结果判断
- 现在平台里已经不再是“需求单”和“回执消息”两张皮。
- 需求页能读到某条需求最近有没有人/线程回应。
- 当前截图里 `按需求看回执` 还是空态，原因是项目里还没有新的真实需求回执数据，不是前端链路没接上。

## 本轮补充 - 临时回执验证与清理

### 做了什么
- 为现有两条真实需求临时补写了两条最小回执：
  - 一条 AI 回执
  - 一条人工回执
- 目的不是留在项目里，而是验证“需求 -> 最近回执”这条后端链路。

### 结论
- 数据库层已经能正确把 `requirement_id` 关联到 `collaboration_messages`。
- 这说明后端和前端的映射字段已经接通。
- 但当前我从 shell 侧直接抓取项目页 HTML 时，没有登录态，拿不到这些受权限控制的真实项目需求数据；所以这轮没把“带真实回执的页面截图”作为最终证据使用。

### 清理
- 两条临时验证消息已删除：
  - `tmp-msg-ai-reply-20260421`
  - `tmp-msg-human-reply-20260421`
- 回查结果：数据库残留 `0`

### 后续建议
- 后面如果继续验“需求 -> 回执 -> 页面显示”，优先在真实登录态浏览器里走，不再依赖无登录态 HTML 抓取。


## ???? - ???????????
### ??????
- ??? `project-playable-shell.tsx` ???????????????
  - `shortText()` ??????????
  - `requirementMessageMap` ? `return bucket` ????????
  - `machine-room` ? `exchange` ????????????????
- `????` ??????????
  - `??? / AI ?? / ????` ?????
  - `????`
  - `??????`
  - `??????`
  - `???`
- `??????` ????????????
  - ????
  - ????
  - ????
  - ????????

### ??
- `npm run build:web` ??
- `python -m pytest tests -q` ???`72 passed`

### ????
- ????????????
- `????` ?????????????????????????????????
- ??????????????????????????? demo ???


## ???? - Git ???????
### ??
- `Git ?? -> ?? AI / ???` ????????? `???? -> ??????` ??????????
  - ????
  - ????
  - ????
- ?????????????????????????????????

### ??
- `npm run build:web` ??
- `python -m pytest tests -q` ???`72 passed`

### ????
- Git ????????????????
- ???????????????? AI ???????????????????????


## ???? - ?????????????
### ??
- `Git ??` ??? `?????` ???????????
- ????????????????????
  - `??????` -> ?? AI
  - `?????????` -> ?? AI
  - `?????????` -> ???
- ?????? `?????` ?????????????????? NPC ????????????????????

### ??
- `npm run build:web` ??
- `python -m pytest tests -q` ???`72 passed`
- `3086` ???????

### ????
- ???????????????????????????????????
- ????????????????????????????????????????????

## 2026-04-21 08:16 继续收口 - 接单状态与真实登录态验证

### 本轮改动
- `apps/web/app/projects/[id]/project-playable-shell.tsx`
  - 新增 `describeRequirementProgress()`，把需求状态压成更直观的工作流语言：`待接单 / AI 已接单 / 人工已接单 / 处理中 / 已完成`
  - `信息交流 -> 按需求看回执` 现在每条需求额外展示：
    - 目标对象
    - 接单/处理状态
    - 最近回执标签
  - `Git 合作 -> 分给 AI / 分给人` 的最近需求卡，也统一增加了接单/处理状态 chip，减少用户自己脑补“谁接了、卡在哪”

### 验证
- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`
- 重启 `3086` 生产包后，用临时账号走真实登录态验证：
  - 临时注册用户
  - 临时加入项目 `1d243fb6-a146-4985-b6ab-a41ac30577a2`
  - 通过 `/api/auth/session` 取会话 token
  - 抓取真实登录态 HTML 验证 Git 合作页关键入口
- 已确认真实登录态 HTML 包含：
  - `维护员派单`
  - `平台主链自检`
  - `分给 AI`
  - `分给人`

### 进一步的临时数据验证
- 为了验证 `处理中 / 已完成` 这类状态展示，临时写入了一条 AI 处理中需求和一条人工已完成需求，并补了一条最小回执
- 真实登录态页面验证后，这些临时 requirement / collaboration_message 已全部删除
- 临时验证账号和项目成员关系也已全部删除
- 同时清掉了该临时账号相关的 `audit_logs`

### 当前判断
- 平台已经不只是“能建需求单”，而是开始更像：
  - 维护员把需求分给 AI 或人
  - 平台里能直接看到目标对象
  - 平台里能直接看到接单状态和最近回执
- 仍然要继续补的，是把这些状态做得更像用户工作流，而不是几个卡片块并列展示

### 下一步
- 继续收 `信息交流 -> 最小报告中心`，让 `谁接了 / 谁没接 / 最近一句话回执` 更集中
- 尝试补一条真正可复用的浏览器截图链；当前 Playwright CLI 可用，但 npm 临时包环境还没有成功直接 `require('playwright')` 到脚本里

## 2026-04-21 08:21 继续收口 - 按人/按线程看与登录态验证

### 本轮改动
- `apps/web/app/projects/[id]/project-playable-shell.tsx`
  - 在 `信息交流` 页新增 `按人 / 按线程看`
  - 把需求按目标对象归拢，直接显示：
    - 待接单数量
    - 处理中数量
    - 已完成数量
    - 最近一条动态
- 这让用户不只看单条需求，而是能直接看“哪个 AI / 哪个人手上压了多少单”

### 验证
- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`
- 真实登录态验证（使用临时账号 + 临时项目成员关系）确认页面 HTML 已包含：
  - `按人 / 按线程看`
  - `当前是谁在接这些需求`
  - `处理中`
  - `已完成`
- 验证产物：
  - `D:\ai合作产品\artifacts\team-exchange-auth-verify-2026-04-21.html`

### 清理
- 临时注册账号 `codex.verify.20260421082102@local.dev` 已删除
- 对应项目成员关系已删除
- 对应 `audit_logs` 已删除
- 没有留下 requirement / message 验证脏数据

### 当前判断
- 平台已经开始从“分流需求”变成“能看到谁在处理、谁还没接”的真正协作界面
- 现在最需要继续补的，是把这些状态再压成更像用户工作流的最小报告中心，并继续补可重复截图链

## 2026-04-21 08:35 继续收口 - 最小报告中心落地并完成登录态验证

### 本轮改动
- `apps/web/app/projects/[id]/project-playable-shell.tsx`
  - 在 `信息交流` 页新增 `最小报告中心`
  - 现在顶部会直接显示：
    - 最新 AI 回执
    - 最新人工回执
    - 最新维护动态
  - 继续保留并增强 `按人 / 按线程看`，把需求按目标对象归拢展示

### 验证
- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`
- 真实登录态验证（临时账号 + 临时项目成员关系）确认页面 HTML 已包含：
  - `最小报告中心`
  - `最新 AI 回执`
  - `最新人工回执`
  - `最新维护动态`
  - `按人 / 按线程看`
  - `当前是谁在接这些需求`
- 验证产物：
  - `D:\ai合作产品\artifacts\team-exchange-auth-verify-2026-04-21.html`

### 清理
- 临时验证账号 `codex.verify.20260421083516@local.dev` 已删除
- 对应项目成员关系已删除
- 对应 `audit_logs` 已删除
- 本轮没有留下 requirement / message demo 残留

### 当前判断
- `信息交流` 已经不再只是消息列表，而开始像真正的最小报告中心
- 用户现在能先看三类最新动态，再看按人/按线程的接单视图，再看单条需求回执
- 后续最值得继续做的是把“哪个线程接了、谁还没接”做成更明显的操作状态，而不是只停在摘要统计

## 2026-04-21 08:35 继续收口 - 最小报告中心落地并完成登录态验证

### 本轮改动
- `apps/web/app/projects/[id]/project-playable-shell.tsx`
  - 在 `信息交流` 页新增 `最小报告中心`
  - 现在顶部会直接显示：
    - 最新 AI 回执
    - 最新人工回执
    - 最新维护动态
  - 继续保留并增强 `按人 / 按线程看`，把需求按目标对象归拢展示

### 验证
- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`
- 真实登录态验证（临时账号 + 临时项目成员关系）确认页面 HTML 已包含：
  - `最小报告中心`
  - `最新 AI 回执`
  - `最新人工回执`
  - `最新维护动态`
  - `按人 / 按线程看`
  - `当前是谁在接这些需求`
- 验证产物：
  - `D:\ai合作产品\artifacts\team-exchange-auth-verify-2026-04-21.html`

### 清理
- 临时验证账号 `codex.verify.20260421083516@local.dev` 已删除
- 对应项目成员关系已删除
- 对应 `audit_logs` 已删除
- 本轮没有留下 requirement / message demo 残留

### 当前判断
- `信息交流` 已经不再只是消息列表，而开始像真正的最小报告中心
- 用户现在能先看三类最新动态，再看按人/按线程的接单视图，再看单条需求回执
- 后续最值得继续做的是把“哪个线程接了、谁还没接”做成更明显的操作状态，而不是只停在摘要统计
