# Codex Platform Maintenance Progress - 2026-04-21 Late

## 当前目标

继续把平台收成“用户在平台里拉多个线程协作维护和更新平台自己”的工作流，同时维持 UTF-8 干净状态，不让乱码继续回流。

## 当前已完成

### 1. 项目规范层已收稳

- `D:\ai合作产品\skills\utf8-guardrail\SKILL.md` 已重建为干净 UTF-8
- `D:\ai合作产品\skills\ai-game-backpack-collab\SKILL.md` 已重建为干净 UTF-8
- 当前约束：
  - 中文文件统一 UTF-8
  - 手工改代码优先 `apply_patch`
  - 验证 demo 做完必须删

### 2. 团队背包已经形成 6 个分舱

项目页团队背包当前固定为：

- `电脑接入`
- `NPC 创建`
- `机房`
- `信息交流`
- `Git 合作`
- `Skill 库`

核心文件：

- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.tsx`
- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.module.css`
- `D:\ai合作产品\apps\web\app\actions.ts`

### 3. 需求分流与最小回执链已经接通

现在平台里的需求链支持：

- `分给 AI`
- `分给人`
- `登记已接单`
- `登记已完成`

实现原则：

- `已接单 / 已完成` 先走最小回执消息
- 不直接放开 `requirement.accept / requirement.close` 的权限门槛
- 页面状态由最新回执消息推导

也就是：

- 最新回执状态是 `in_progress` -> 页面显示 `AI 已接单 / 人工已接单`
- 最新回执状态是 `done` -> 页面显示 `已完成`

### 4. 信息交流已经开始像最小报告中心

`信息交流` 当前已经有：

- `最小报告中心`
- `平台维护看板`
- `发送命令`
- `按需求看回执`
- `按人 / 按线程看`
- `收件箱`

并且页面里已经能直接看到：

- `派给 AI`
- `派给人`
- `已接单`
- `已完成回执`
- `AI 无人接`
- `AI 处理中`
- `人工处理中`
- `最近活跃目标`

### 5. Git 合作页已经是需求分流和协作桥

`Git 合作` 当前已经包含：

- `Git 配置`
- `分给 AI`
- `分给人`
- `最近需求回执摘要`
- `Git 活动`
- `维护员派单`
- `平台自维护状态`

### 6. 平台自维护任务已形成固定模板

当前固定维护任务：

- `平台主链自检`
- `复查电脑与线程扫描`
- `人工确认平台风险点`

并且仍然支持就地快速派发：

- `平台主链自检 -> 立即派给 AI`
- `复查电脑与线程扫描 -> 立即派给 AI`
- `人工确认平台风险点 -> 立即派给人`

## 本轮新增

### 协作闭环总览

在 `信息交流` 顶部新增了更直白的闭环总览卡：

- `派给 AI`
- `派给人`
- `已接单`
- `已完成回执`

目的：

- 一眼看出任务先流向哪条链
- 一眼看出已经有多少需求被正式接住
- 一眼看出完成回执积累到了多少

这样用户打开平台时，不用先逐条翻需求卡，也能先判断现在该补派单、等回执，还是看完成线。

## 验证

### 前端构建

- 命令：`npm run build:web`
- 结果：通过

### 后端测试

- 命令：`python -m pytest tests -q`
- 结果：`72 passed`

### 需求最小回执 E2E

已做过一次真实临时闭环验证，并清掉残留：

1. 临时注册成员
2. 临时加入当前项目
3. 创建一条临时 AI 需求
4. 写入一条 `已接单` 最小回执
5. 写入一条 `已完成` 最小回执
6. 校验数据库
7. 删除临时需求、临时回执、临时账号、临时成员关系

验证产物：

- `D:\ai合作产品\artifacts\requirement-ack-e2e-2026-04-21.json`

清理结果：

- `remaining_requirements = 0`
- `remaining_messages = 0`
- `remaining_user = 0`
- `remaining_membership = 0`

### 页面级验证

当前重点页面：

- `http://127.0.0.1:3086/projects/1d243fb6-a146-4985-b6ab-a41ac30577a2?panel=team&tab=exchange`
- `http://127.0.0.1:3086/projects/1d243fb6-a146-4985-b6ab-a41ac30577a2?panel=team&tab=git`

最近已确认页面中存在：

- `平台维护看板`
- `立即派给 AI`
- `当前是谁在接这些需求`
- `按人 / 按线程看`
- `AI 链待接`
- `人工链待接`
- `已回执`
- `维护员派单`

## 当前判断

平台已经从“能建需求单”推进到“能在平台里看到任务流向、最小回执和当前谁在处理”。

离最终目标还差的重点不是基础链路，而是：

1. 真实登录态下的可视化验证还要继续补
2. `分给 AI / 分给人 -> 接单 -> 回执 -> 状态变化` 还要再压成更明显的用户闭环
3. 需要继续让平台更像“我在平台里拉线程一起维护平台”，而不是“我在后台脑补状态”

## 下一步

1. 继续做真实登录态下的页面级验证和截图。
2. 把 `协作闭环总览` 再同步进 `Git 合作`，形成前后呼应。
3. 继续补“哪个线程接了、谁还没接、谁已完成”的更直白提示。

## 本轮继续补充 - 协作闭环总览

### 新增页面能力

这轮把“派给谁、谁接了、谁完成了”又往前收了一层：

- `信息交流` 顶部新增 `协作闭环总览`
  - `派给 AI`
  - `派给人`
  - `已接单`
  - `已完成回执`
- `Git 合作` 页也同步新增 `Git 协作闭环`
  - 让用户不用切到另一页脑内换模型
  - 直接在 Git 分流页也能看到同一套闭环统计

目的：

- 一眼看出任务总量先流向哪条链
- 一眼看出已经有多少需求被正式接住
- 一眼看出完成回执积累到了多少

### 验证

- `npm run build:web` 通过
- 源码级校验确认以下新文案已经写入：
  - `协作闭环总览`
  - `Git 协作闭环`
  - `派给 AI`
  - `已完成回执`

### 当前判断

平台现在比上一轮更接近“用户真在拉多个线程协作”的状态：

- `信息交流` 更像调度台
- `Git 合作` 更像分流和回执桥
- 两边都开始围绕同一套闭环状态说话，而不是各自一堆卡片

## 本轮继续补充 - 当前推荐操作

### 新增页面能力

为了让平台更像“我一打开就知道下一步要盯哪条线”，这轮又补了一层：

- `信息交流` 新增 `当前推荐操作`
- `Git 合作` 也同步新增 `当前推荐操作`

推荐规则：

- 如果还有未派单的维护任务，优先提示：
  - `先处理维护任务：平台主链自检`
  - 或其他未派单维护项
- 否则优先盯住当前最忙的目标，直接带上建议动作：
  - `优先盯住 xxx：优先补派单或催第一次回执`
  - `优先盯住 xxx：继续等处理中回执，必要时催一下`

### 验证

- 初次补入时前端构建报错：
  - `maintenanceDispatchBoard used before declaration`
- 已修复变量声明顺序后重新验证：
  - `npm run build:web` 通过

### 当前判断

平台现在不只是展示状态，而是开始主动告诉用户“先做什么”。这更接近最终目标：你作为平台用户，应该是在平台里拉线程协作，而不是自己先脑补优先级。

## 本轮继续补充 - 当前推荐操作可跳转

### 新增页面能力

这轮把 `当前推荐操作` 从“只会提示”推进成“可以直接带用户去下一步该去的分舱”：

- 如果还有未派单维护任务：
  - `当前推荐操作` 下会直接出现 `去 Git 合作派单`
- 如果维护任务已经派出，但仍有重点目标要盯：
  - `当前推荐操作` 下会直接出现 `去信息交流盯进度`

这样用户打开平台时，不用自己先判断应该切到哪个页签，平台会把最合适的下一步入口直接给出来。

### 验证

- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`

### 当前判断

平台开始更像一个真实调度台，而不是状态看板：

- 先告诉你当前最该做什么
- 再直接把你送到该去的分舱

## 本轮继续补充 - 目标级协作提示

### 新增页面能力

为了让用户不只是知道“有多少需求”，还知道“是谁在推进”，这轮又补了 3 张目标级状态卡：

- `当前在接单`
- `等待第一次回执`
- `最近完成目标`

这 3 张卡现在在：

- `信息交流`
- `Git 合作`

里都会出现，展示逻辑来自 `requirementOwnershipBoard`：

- 谁当前有处理中需求
- 谁还有待接需求
- 谁最近已经完成了一条线

同时每张卡都会带上当前建议动作，不再只是名字。

### 验证

- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`

### 当前判断

平台现在比上一轮更接近“像用户在拉团队干活”：

- 不只是看总量
- 还能直接知道当前谁在接单
- 谁还没回第一条回执
- 哪条线已经开始收口

## 本轮继续补充 - 无人接单目标与最近完成回执

### 新增页面能力

这轮继续把信息交流收成更像真实调度台，新增了两组更直白的列表：

- `无人接单目标`
- `最近完成回执`

作用：

- `无人接单目标`
  - 直接列出当前还没人明确接住的目标
  - 不用自己从 `按人 / 按线程看` 里翻
- `最近完成回执`
  - 直接列出最近已经完成并有回执的需求
  - 不用自己从全部需求里找哪条线已经收口

### 验证

- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`

### 当前判断

平台现在更接近“用户直接调度协作”：

- 先看还有哪些目标没人接
- 再看哪些线已经完成
- 再决定是补派单、催回执，还是切到下一条线

## 本轮继续补充 - 列表内直接跳下一步

### 新增页面能力

这轮继续把“下一步该去哪儿”从顶部提示，推进到了具体列表项里：

- `无人接单目标`
  - 现在会直接给出 `去 Git 合作补派单`
- `按人 / 按线程看`
  - 会根据当前状态给出：
    - `去 Git 合作补派单`
    - `去信息交流看回执`
    - `去 Git 合作看结果`
- `最近完成回执`
  - 也能直接跳去 `Git 合作看结果`

也就是说，平台现在不只是告诉你“哪个目标有问题”，还会把你直接送到下一步该做事的分舱。

### 验证

- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`

### 当前判断

平台已经开始从“状态看板”变成“可操作调度台”：

- 列表里就能做下一步决策
- 不需要用户再自己记住目标名称然后切页

## 本轮继续补充 - Git 页同等级列表

### 新增页面能力

之前 `无人接单目标` 和 `最近完成回执` 只在 `信息交流` 里。  
这轮我把它们也补到了 `Git 合作`：

- `最近接单快照`
  - 现在同样带动作按钮
- `无人接单目标`
  - 现在在 `Git 合作` 里也能直接看到
- `最近完成回执`
  - 现在在 `Git 合作` 里也能直接看到

这样你就算主要站在 `Git 合作` 这一页，也不需要再切到 `信息交流` 才知道：

- 哪些目标还没人接
- 哪些线已经完成
- 下一步该去补派单还是去看结果

### 验证

- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`

### 当前判断

`信息交流` 和 `Git 合作` 现在更接近同等级工作面板，而不是一个强一个弱。用户无论站在哪个分舱里，都能比较完整地继续调度。 
