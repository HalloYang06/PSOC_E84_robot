# Codex 项目页外壳重建交接 - 2026-04-21

## 本轮目标
- 把已经损坏并出现大量乱码的 `project-playable-shell.tsx` 整体救回来。
- 保留三页主线和项目游戏页里的团队背包逻辑。
- 继续维持 6 个分舱：
  - 电脑接入
  - NPC 创建
  - 机房
  - 信息交流
  - Git 合作
  - Skill 库
- 保证 UTF-8 中文正常，不再出现新的乱码字符串。

## 实际完成

### 1. 整体重建项目页外壳
文件：
- `D:\ai合作产品\apps\web\app\projects\[id]\project-playable-shell.tsx`

处理方式：
- 直接整文件重写，不再继续补丁式修坏掉的 JSX。
- 保留并重新接回：
  - 游戏 iframe
  - 顶部项目栏
  - 游戏内背包按钮
  - 右侧团队背包
  - 6 个页签分舱

### 2. 干净中文重新落回页面
当前已确认在线的中文内容包括：
- `电脑接入`
- `NPC 创建`
- `机房`
- `信息交流`
- `Git 合作`
- `Skill 库`
- `退出登录并重测`
- `最小报告中心`
- `按人 / 按线程看`
- `维护员派单`
- `平台主链自检`
- `复查电脑与线程扫描`
- `人工确认平台风险点`

### 3. 信息交流页恢复并继续补强
已恢复：
- 最小报告中心
- 最新 AI 回执
- 最新人工回执
- 最新维护动态
- 按需求看回执

新增：
- `按人 / 按线程看`
  - 聚合显示每个目标对象的：
    - 待接单
    - 处理中
    - 已完成
    - 最近动态

### 4. Git 合作页恢复并继续补强
已恢复：
- Git 配置
- 分给 AI
- 分给人
- 最近需求回执摘要
- Git 活动

新增：
- `维护员派单`
  - 平台主链自检
  - 复查电脑与线程扫描
  - 人工确认平台风险点

## 验证

### 语法解析
在 `apps/web` 目录下用 TypeScript 解析 `project-playable-shell.tsx`：
- 结果：`parseDiagnostics = []`

### 前端构建
- 命令：`npm run build:web`
- 结果：通过

### 后端测试
- 命令：`python -m pytest tests -q`
- 结果：`72 passed`

### 页面级验证
服务：
- `http://127.0.0.1:3086`

验证方式：
- 抓取真实 HTML 并检查关键中文是否在线
- 验证文件：
  - `D:\ai合作产品\artifacts\team-shell-html-verify-2026-04-21-b.html`

验证结果：
- 默认 `?panel=team` 页面能看到：
  - 6 个分舱
  - `退出登录并重测`
- `?panel=team&tab=exchange` 页面能看到：
  - `最小报告中心`
  - `按人 / 按线程看`
  - `最新 AI 回执`
  - `最新人工回执`
  - `最新维护动态`
- `?panel=team&tab=git` 页面能看到：
  - `维护员派单`
  - `平台主链自检`
  - `复查电脑与线程扫描`
  - `人工确认平台风险点`
  - `Git 配置`
  - `分给 AI`
  - `分给人`

### 乱码检查
对默认页、`exchange`、`git` 三个 HTML 抓取结果做乱码关键词扫描：
- 未发现：
  - `鏈`
  - `锛`
  - `脳`
  - `鍖`
  - `鏆`

## 当前状态判断
- 这轮最关键的问题已经解决：
  - 项目页外壳重新可维护
  - 构建恢复
  - 中文恢复
  - 主要工作流重新上线
- 现在页面不再卡死在乱码和坏 JSX 状态。

## 下一步建议
- 继续在真实登录态下补浏览器可视化验证，不只抓 HTML。
- 把 `Git 合作 -> 分给 AI / 分给人 -> 信息交流回执` 的链路再做更强的状态变化展示。
- 继续把地图里的 NPC 交互和背包页的 NPC 属性保持一致。

## 本轮继续补充 - 平台自维护状态板与稳定启动

### 新增页面能力
- `信息交流` 页签新增更直观的状态入口：
  - `当前是谁在接这些需求`
  - `待接单`
  - `处理中`
  - `已完成`
- `Git 合作` 页签新增更明显的维护态摘要：
  - `AI 链待接`
  - `人工链待接`
  - `已回执`
  - `维护员派单`

### 重新验证
- `npm run build:web` 通过
- `python -m pytest tests -q` 通过，`72 passed`
- 重新从 `apps/web` 目录单独启动：
  - `set PORT=3086 && npm run start`
- 当前 `3086` 已恢复稳定响应

### 页面级校验
- `exchange` 页 HTML 已确认出现：
  - `当前是谁在接这些需求`
  - `待接单`
  - `处理中`
  - `已完成`
- `git` 页 HTML 已确认出现：
  - `AI 链待接`
  - `人工链待接`
  - `已回执`
  - `维护员派单`

### 截图
- `D:\ai合作产品\artifacts\team-git-workflow-verify-2026-04-21-e.png`

### 当前判断
- 项目页外壳已重新可维护。
- 平台自维护工作流已经从“说明文字”推进到“状态板 + 派单入口”。
- 当前最大的剩余项不再是乱码和坏 JSX，而是：
  - 真实登录态下的可视化验证还要继续补
  - `分给 AI / 分给人 -> 回执 -> 状态变化` 还可以继续做得更直观
