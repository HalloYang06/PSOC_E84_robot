# Troubleshooting and Lessons

## 2026-07-04

- Legacy Bluetooth lesson: the old tested rehab-arm App protocol is Bluetooth Classic SPP/RFCOMM, not BLE GATT. It uses UUID `00001101-0000-1000-8000-00805F9B34FB`, UTF-8 JSON, and `\n` packet framing. A browser/PWA cannot open this RFCOMM link directly, so the Android package needs a native Capacitor/Kotlin bridge before phone-to-M33 communication is real.
- Protocol safety lesson: do not convert App hello/status/diagnostic requests into guessed old-protocol packets. The old protocol formally defines `mode`, `control`, `memory`, `execute_memory`, and `stop`; only send compatible frames with `sendable=true`, and keep unsupported status-like messages as backend evidence until M33 firmware confirms a command.
- Workflow-form lesson: phone workflow buttons are not closed-loop if each frontend shell guesses the payload shape. Return `payload_schema` and `form_contract` with each `/me/workflow.action_queue` item so Stitch/PWA/APK can render fields from backend-authored rules and submit through `/me/workflow/actions`.
- Workflow-preflight lesson: `PREFLIGHT_CHECK_REQUIRED` can be App-executable only after the current plan version has real `m33_accepted` evidence. Execute it by reusing the normal preflight service so pain thresholds, checklist completeness, sync id, and plan-version gates stay centralized.
- CORS QA lesson: local static QA from `127.0.0.1` can fail against the cloud API preflight even when the same APK/PWA works from the deployed `106.55.62.122:3001` origin. For workflow-panel QA, use the cloud PWA route after deploying static assets, or explicitly configure a matching allowed origin; do not treat local `Failed to fetch` as a workflow contract failure.

## 2026-07-03

- Workflow contract lesson: phone screens should consume `/api/rehab-arm/app/v1/me/workflow` for `phase`, `next_action`, `action_queue`, and `blockers` instead of inferring state from scattered records or static copy. Keep the endpoint evidence-only and preserve `forbidden_actions` so frontend work cannot accidentally imply App-granted motion permission.
- Mobile PWA QA lesson: `/rehab-arm-mobile/index.html` is the static landing entry, while the backend bridge/login QA surface is `/rehab-arm-mobile/home.html`. Use `home.html` when validating token login, `/me` bootstrap, evidence panel insertion, and static-copy suppression.
- QA lesson: a phone App can be technically connected to `/me` while still feeling like a demo if old static success labels remain visible. Treat optimistic copy such as `M33 ACTIVE`, `已允许执行`, fixed completion percentages, or confident AI recommendations as QA failures unless they are backed by persisted backend evidence.
- APK delivery lesson: backend readiness fields are invisible to a phone user until the packaged WebView actually has an API base, Bearer-token login, and `/me` bootstrap wiring. For the current HTTP cloud API, the debug wrapper also needs `android:usesCleartextTraffic="true"`; otherwise Android WebView can block the request even when CORS passes.
- Catalog lesson: phone training-plan forms should not hard-code demo movement strings. Publish backend-authored movement/profile catalogs and reject unsupported `movement_type` values before M33 sync so the App cannot create plausible-looking but unreviewable demo plans.
- Cloud test lesson: the Tencent host does not expose `python` or global `pytest` for the API test suite. Use `apps/api/.venv/bin/python -m pytest ...` when validating cloud-synced rehab-arm App backend files.
- Mobile release lesson: do not publish a new APK as user-ready just because backend endpoints pass tests. The public config and authenticated `/me.mobile_readiness_guide` must keep saying `blocked` while the Android wrapper still falls back to static preview data or lacks `Authorization: Bearer {access_token}` bootstrap wiring.
- Hardware protocol lesson: missing BLE/M33/M55 packet maps are a product blocker, not a reason to invent demo behavior. The App backend should expose `awaiting_protocol` readiness and continue recording plans, diagnostics, evidence, and safety reviews without pretending it can certify motion.
## 2026-06-17

- Lesson: for a first-pass vision stack, two RGB cameras are enough for coarse stereo + YOLO perception, but not for trustworthy depth or motion authority.
- Lesson: keep the stereo output as a dedicated perception record (`stereo_vision_context`) instead of overloading single camera keyframes.
- Lesson: the platform should prefer stereo vision context for VLA vision input, but must still stop at high-level suggestions.
- Validation note: the stereo context path passed backend tests after adding a new request schema and dashboard wiring.
- Lesson: the command-center human muscle view must not hand-draw an anatomy model. Keep it as a GLB/GLTF asset slot with documented open-source sources and let EMG/action-prediction data render as overlays/cards.
- QA note: local visual QA requires both web `:3000` and API `:8011`; without the API the login flow stays on `/login`, and with the wrong project id it correctly shows "项目不存在或无权限".

## 2026-06-27

- Symptom: local rehab-arm screenshot QA against `127.0.0.1:3000` redirected to `/login` even after cloud `/api/auth/session` returned a valid `farm_access_token`.
- Environment: local Next dev server, cloud API `http://106.55.62.122:8011`, rehab-arm route `/projects/e201f41c-25a6-46e1-baf8-be6dcb83284c/rehab-arm-control`.
- Lesson: authenticated screenshot helpers that inject cookies after browser startup may not satisfy Next SSR route guards on the first document request. Use the production cloud page after deployment, or run local API/auth with matching environment, before claiming user-view QA.
- Status: QA limitation recorded; backend contract and frontend build still passed for the visual-lock-meter slice.
- Correction: the cloud test account password is `1234`, not the previously used `password`. Cloud `/api/auth/session` succeeds with `3245056131@qq.com` / `1234`, and the production rehab-arm page can be opened through the normal login form.
- Deployment note: cloud HTTP health was OK, but non-interactive SSH push/deploy was blocked by `Permission denied (publickey,password)`. Use an interactive SSH session or restore key-based auth before claiming the latest web build is deployed.
- Fix: the working cloud SSH path is `ssh -i .codex-cloud-ssh/tencent_lighthouse_ed25519 -o UserKnownHostsFile=.codex-cloud-ssh/known_hosts ubuntu@106.55.62.122`; root login is not the expected deploy path.

## 2026-07-01

- Lesson: when the camera cannot see the real target and end effector in the same frame, use a short visual memory state instead of blocking the whole VLA-lite demo. Keep the TTL small; the current platform UI treats 5 seconds as usable for dry-run evidence only.
- Safety boundary: visual memory is not a hand-eye calibration, not a 3D robot-frame coordinate, and not motion permission. It can explain why A continues observing or prepares a dry-run candidate, but real motion still requires fresh perception, calibration, simulation/human review, and M33 authority.
- Symptom: the rehab-arm human muscle panel created a Three.js canvas but still showed a GLB placeholder, even though `/assets/human/*.glb` returned HTTP 200 on the cloud host.
- Root cause: the open anatomy GLB files use `KHR_draco_mesh_compression`; the cloud host did not serve `/assets/draco/gltf/draco_decoder.wasm` and `draco_wasm_wrapper.js`, so `GLTFLoader` failed after fetching the GLB.
- Fix: deploy `apps/web/public/assets/draco/gltf/*` with the web static assets and verify the decoder URLs return 200 before judging the GLB viewer. The loader should also use partial-success semantics so one failed model layer does not hide all successfully loaded layers.
- Lesson: if a GLB canvas is present but the model is invisible, check dependent decoders/textures and then fit the camera from the loaded model bounds. Do not assume a fixed camera works for anatomy assets, because their long axis may be Y rather than the expected Z-up display orientation.

## 2026-07-02

- Stitch MCP connection note: the configured Stitch MCP is available through `mcp__stitch`. The owned project titled `灵动康复 ArmControl` resolves to `projects/9733571660387876930`; shared projects returned empty during this check.
- Lesson: do not assume the requested `docs/AI_PROJECT_STRUCTURE_GITHUB.md` exists in the AI collaboration platform repo. When missing, use the current rehab-arm architecture sources (`medical-rehab-arm-platform-development-plan.md`, `platform-agent-operating-architecture.md`, `rehab-arm-nanopi-vla-mujoco-integration.md`, and the app backend prompt) and record the substitution.
- Safety lesson: Stitch mobile screens may contain control-like UX, but backend APIs must phrase all action-adjacent flows as plan sync, M33 decision status, or evidence upload. Syncing a training plan means M33 received structured data; it is not motion authorization.
- Mobile delivery lesson: do not describe the phone APP as Python. The Python/FastAPI code is a backend API. The phone-facing target should be a PWA/mobile Web shell first, then optionally an Android wrapper or Kotlin/Compose app.
- QA note: the Stitch-generated HTML uses `cdn.tailwindcss.com`, so browser QA logs Tailwind CDN production warnings. This is acceptable for design/PWA preview, but a production/offline phone package should localize or compile the CSS instead of depending on the CDN.
- Existing regression note: `python -m pytest tests/test_rehab_arm_sync.py -q` currently fails in `test_rehab_arm_model_relay_a_semantic_routes_all_reserved_modes`, where the prompt `进入安全审核，确认仿真和急停状态` routes to `diagnostics` instead of expected `safety_review`. This appears unrelated to the new mobile APP backend/PWA slice but should be fixed before claiming the whole rehab-arm backend suite is green.
- Symptom: after the control-room stage refactor, local Playwright QA stayed on `/login` even with the right account form values, while the cloud CDP helper opened the authenticated page successfully.
- Root cause: the platform auth cookie names are `farm_access_token` and `farm_user`; guessing generic cookie names such as `codex_access_token` does not satisfy route guards. Local web without a matching local API/session also stays on the login page.
- Fix: for cloud user-view QA, use `scripts/capture-auth-screenshot-cdp.py --api-base http://106.55.62.122:8011 --login-email 3245056131@qq.com --login-password 1234`; it injects the correct `farm_*` cookies before navigation.
- Symptom: the first muscle-focus screenshot showed only the Three.js grid and dim labels, then a later 30-second capture showed the GLB model correctly.
- Lesson: anatomy GLB plus Draco decode can be slower than the rest of the page after deploy/restart. Do not conclude the model is broken from an early screenshot alone; wait long enough or inspect loader errors.
- Lesson: when a muscle label cannot be bound to a named mesh in the current GLB, dim it as an unbound/fallback label. Hard-positioning a label to "look right" is worse for a competition demo because it implies false anatomical precision.
- Symptom: uploading `apps/web/public/previews/rehab-arm-control-room/` to the cloud initially failed because `/home/ubuntu/apps/ai-collab/apps/web/public/previews/` did not exist.
- Fix: create the remote parent first with `mkdir -p /home/ubuntu/apps/ai-collab/apps/web/public/previews`, then `scp -r` the preview directory.
- Symptom: `http://106.55.62.122:3001/previews/rehab-arm-control-room/` returned the app-level not-found page even though the static files existed.
- Root cause: this Next deployment does not serve `public` subdirectories as directory indexes after redirecting away from the trailing slash.
- Fix: use the explicit static file URL `http://106.55.62.122:3001/previews/rehab-arm-control-room/index.html` for cloud review and screenshot QA.
- Lesson: when wrapping Stitch-generated mobile screens in an iframe PWA shell, bottom-nav clicks inside the iframe do not automatically update the outer shell. Use a small bridge script plus `postMessage` so each Stitch page remains visually intact while navigation stays phone-runnable.
- Lesson: keep the mobile bridge action-adjacent but not authority-adjacent. Buttons such as start, sync, calibrate, and stop should route, record intent, or submit evidence only; they must keep explaining that M33 is required before motion and the App cannot release emergency stop.
- Environment note: `winget install EclipseAdoptium.Temurin.21.JDK` stalled under `winget/msiexec`, so the Android build environment uses a zip-installed Temurin JDK 21 at `D:\Java\jdk-21` instead. Use `scripts/use-android-build-env.ps1` in new PowerShell sessions before running Capacitor/Gradle commands.
- Android SDK lesson: `sdkmanager --licenses` needs explicit `y` input in non-interactive PowerShell. Pipe repeated `y` values before installing packages, otherwise `platform-tools`, build-tools, and platforms are skipped even though the command exits cleanly.
- Build lesson: Android Gradle on Windows refuses project paths with non-ASCII characters by default. Because this repo lives under `D:\ai合作产品`, the Capacitor Android project needs `android.overridePathCheck=true` in `android/gradle.properties`.
- Deployment lesson: uploading a new `public/` subdirectory to the cloud Next app may return 404 until the Next process restarts. Restart with `RESTART=1 bash scripts/start-cloud-prod.sh` before validating new APK/PWA static paths.
- Backend product lesson: a phone App can look closed-loop while still being a demo if `training-sessions/start` creates records before M33 has accepted the plan. The backend must check the selected plan/device pair and reject starts unless the latest current-version sync is `m33_accepted`.
- Safety lesson: M33 acceptance is version-specific. If a therapist or AI draft edit changes sets/reps/assist/safety constraints, increment the training plan version and require a fresh sync plus M33 acceptance before a new session record can start.
- SQLite migration lesson: `Base.metadata.create_all` creates missing tables but does not add columns to existing tables. When adding `rehab_app_training_plan_syncs.plan_version`, also extend `ensure_schema_extensions()` or existing local/cloud SQLite databases will fail after deployment.
- Offline queue safety lesson: phone offline replay must be a whitelist, not a generic command bus. Allow evidence/review operations only, and reject anything resembling motor, torque, current, raw pose/velocity, CAN, M33 override, or emergency-stop release.
- Test lesson: after a pytest timeout on Windows, check for stale `python -m pytest ...` processes before rerunning the full backend file. A stale process can keep a temp SQLite DB active and make the next run look like a product hang.
- JSON persistence lesson: do not store full response payloads with datetimes inside SQLite JSON fields for queue replay results. Store compact replay status, resource id, and control boundary instead.
- Product loop lesson: a training report should be generated only after `training_sessions.status == finished`. If the App can generate a report before finish, it becomes another demo artifact instead of a trustworthy post-session review record.
- BLE contract lesson: treat App-to-M33 BLE payloads as a strict whitelist. Generate structured training/status/diagnostic messages and record M33 ACK evidence, but reject any payload or ACK that contains CAN, motor, torque/current, raw motion, M33 override, or emergency-stop release fields.
- SQLite timestamp lesson: server defaults may only have second-level precision, so "latest" queries can be unstable when several records are created in one test/request burst. For phone-visible latest EMG/M55 summaries, write application-layer UTC timestamps.
- Report review product lesson: keep post-session review separate from plan creation and M33 acceptance. A therapist note or "request new plan" flag is useful product state, but it must not silently become a new executable plan or motion authorization.
- JSON draft lesson: report/review response dictionaries can contain `datetime` values. Before persisting a report-derived context snapshot or generated plan into SQLite JSON fields, recursively convert datetimes to ISO strings or the draft generation endpoint will fail during commit/serialization.
- Test isolation lesson: rehab-arm App backend tests should register a unique user per test. Reusing the default login can mix persisted drafts/sessions from earlier tests and make list/latest assertions fail for the wrong product reason.
- SQLite ordering lesson: AI draft recovery should write application-layer UTC `created_at` and order by `created_at desc, id desc`. Server-default second precision can make "latest draft" unstable when tests or phones create several drafts quickly.
- Stitch-first AI module lesson: when the user asks for AI/model UI changes, use Stitch MCP first even if the live function already exists. For the inline model relay module, the durable Stitch reference is `projects/1207434422396149303/screens/3a7d99e6dc1845fab531bec42db44392`; Codex should preserve the existing relay handlers and only adapt the layout/data wiring.
- Next build note: `npm --workspace apps/web run build` may print that TypeScript include was updated for a temporary `.next-build-staging-*` path. Check `git diff -- apps/web/tsconfig.json` before staging; in this run there was no durable tsconfig diff to commit.
- Device lifecycle lesson: phone App "unbind" should not hard-delete a rehab-arm device binding. Freeze it as `trust_status=revoked` so reports, diagnostics, and audit history stay visible, while training sync, BLE, M33 decisions, and session starts remain blocked.
- Session state lesson: App pause/resume/cancel are backend evidence states, not direct robot pause/stop commands. Keep `paused` sessions occupying the device to prevent duplicate starts, and lock `cancelled` sessions from finish/report/progress so abandoned evidence cannot become a fake completed report.
- Training-plan lifecycle lesson: archiving or rejecting a plan must close the action-adjacent path. Keep the record visible for history, but block plan sync and session start with `TRAINING_PLAN_NOT_USABLE`.
- Preflight lesson: M33 acceptance alone is not enough for a user-facing rehab App. Require a current App preflight record tied to the exact plan version, device, and accepted sync id so stale confirmations cannot start a session after plan edits or M33 re-decisions.
- App recovery lesson: paused training sessions are still active user workflows. `/me` should return them as `active_session`, and latest preflight evidence should be readable on bootstrap so a phone restart does not strand the patient between safety check and session recovery.
- Pain-gate lesson: preflight pain scores are safety workflow signals, not medical diagnosis. Use them to block patient-only starts and require therapist review when pain rises above baseline or reaches a high absolute value; do not silently convert a high-pain check into a passed session gate.
- Constraint-gate lesson: profile `medical_constraints` must not stay as passive text. Enforce clear conflicts before plan sync/start, allow explicit therapist review evidence, and still leave M33 as final safety authority.
- Test lesson: preflight records created within the same second can have unstable "latest" ordering on SQLite server defaults. Tests should assert the relevant plan/device/sync evidence exists unless application-layer timestamps are added.
- Constraint-review lesson: do not use a mutable plan JSON flag as a contraindication bypass. Store a separate review record tied to the exact plan version, so later plan edits require a fresh review before M33 sync/start can proceed.
- Session safety lesson: in-session pain, fit, M33 reject, fatigue, and stop-request evidence should be first-class records. High-risk App events may pause the App session state, but they still do not claim robot-side stop authority without M33/firmware evidence.
- SQLite ordering fix: safety event timelines are user-visible, so write application-layer UTC `created_at` when inserting events instead of relying on SQLite server-default second precision.
- Pytest recovery note: if `tests/test_rehab_arm_app_backend.py` appears to hang on Windows, rerun with `-o faulthandler_timeout=60` after checking for stale pytest processes. In this slice the faulthandler run passed, indicating no product deadlock.
- Offline sync lesson: every new App evidence loop needs both platform sync counts and offline replay coverage, otherwise the phone can look closed-loop online while losing therapist/safety evidence after offline use.
- Safety-review gate lesson: critical safety events should block resume and the next start until a later approved/conditional `safety_review` exists. Reviews must remain evidence-only and still do not grant M33 motion authority.
- Training readiness lesson: the phone UI should call the read-only readiness endpoint before `training-sessions/start` instead of letting users discover opaque 409 errors one by one. Readiness is a UX/evidence summary only; `can_start=true` means backend prerequisites are currently satisfied, not that the App has physical motion authority.
- Start-guide product lesson: status checks are not enough for a usable phone App. Pair readiness with an explicit next-action guide so the UI can route the user to preflight, M33 sync, therapist review, or active-session recovery without turning backend 409 codes into mystery dead ends.
- Bootstrap product lesson: the phone home screen should not rebuild the primary training CTA from separate device, plan, session, and preflight lists. Return a backend-composed `primary_start_guide` from `/me` so all frontend shells follow the same safety ordering.
- Onboarding product lesson: first-run users need a backend-authored setup guide, not an empty dashboard. Keep `/me.onboarding_guide` ordered as profile -> trusted M33 device -> usable plan so the frontend cannot accidentally invite training before the safety prerequisites exist.
- Daily-action product lesson: once onboarding and start readiness exist, the phone home screen still needs a single backend-authored daily action. Prioritize unfinished sessions and review/draft loops before normal start guidance so users do not skip required evidence review.
- Care-timeline product lesson: a usable rehab App needs a real history surface, not static demo cards. Build `/me.care_timeline` from persisted sessions, reports, drafts, and offline queue items so the phone can show continuity without inventing progress.
- Care-summary product lesson: home overview counts should be backend-composed from persisted records. Frontends should not independently infer blockers from scattered lists, or different shells will disagree about whether setup, review, draft, offline replay, or active-session recovery needs attention.
- Offline-sync guide lesson: queued offline evidence should have a visible replay plan before users press sync. Expose queued and failed item ids plus the exact replay endpoint from `/me.offline_sync_guide`, while keeping replay restricted to whitelisted evidence operations.
- Session-recovery guide lesson: paused and active sessions need a backend-authored allowed-action list. If a critical safety event is unreviewed, the recovery guide must point to `safety_review` evidence rather than offering a direct resume path.
- Report-followup guide lesson: post-training state should not be rebuilt independently by each phone screen. Return a backend-authored sequence for report review, next-plan draft creation, draft acceptance, and M33 sync so the App stays on evidence workflow guidance instead of implying motion permission.
- Device-operational guide lesson: device cards should not infer hardware readiness from scattered device/sync/diagnostic fields. Return one backend-authored guide for bind, diagnostic upload, M33 pending/rejected/accepted states, and start-readiness checks while keeping all actions evidence-only.
- Safety-review guide lesson: historical critical safety events can block the next start even after the session is cancelled or finished. Expose the blocking session/event in `/me.safety_review_guide` so users are routed to review evidence instead of repeatedly retrying start.
- Accepted-plan guide lesson: accepting an AI draft is not the end of the workflow. Return a backend-authored accepted-plan guide so the phone continues through trusted device, M33 sync/decision, preflight, and start readiness instead of implying that AI acceptance permits exercise.
- Rehab arm app loop lesson: after a session is marked `finished`, the phone must not infer the report-generation endpoint from generic session status. Return an explicit `/me.finished_session_report_guide` with `GENERATE_TRAINING_REPORT`, then clear it once the training report exists so report review can take over. Status: implemented and covered by `tests/test_rehab_arm_app_backend.py`.
- Rehab arm app offline UX lesson: `offline_sync_guide` should expose structured `actions`, not just `next_action` strings plus replay endpoint fields. This avoids frontend endpoint guessing while keeping replay limited to whitelisted evidence operations. Status: implemented and covered in `test_rehab_arm_app_offline_diagnostics_sync_and_audit_loop`.
- Rehab arm QA lesson: web server-side auth reads `farm_access_token`/`farm_user`, not generic `access_token`/`user`. A manual Playwright authenticated context must set the farm cookie names or the project page falls back to the no-access state even when the API token itself is valid. Status: verified during local QA.
- Rehab arm frontend QA lesson: the Stitch/control-room page can be visually present while exposing no readable `innerText` or button/link semantics to browser automation. Capture screenshots plus DOM metrics and send semantic/mobile follow-up to Stitch instead of treating visual presence alone as App readiness.
- Rehab arm offline replay bug: adding `VIEW_OFFLINE_QUEUE` to `offline_sync_guide` was insufficient while `/me` only loaded queued items. Failed replay rows must be included in the bootstrap queue slice or the phone cannot inspect failures. Status: fixed with `_app_offline_queue_items` and test coverage.
- Rehab arm offline action lesson: a guide action is not truly closed-loop unless the target endpoint accepts the hinted parameters. `VIEW_OFFLINE_QUEUE` now has a tested `status=failed` filter, matching the guide payload. Status: fixed.
- Rehab arm guide action lesson: when an action is meant to be tappable, encode required query parameters in `endpoint`, not only in `payload_hint`; hints are useful for UI context but should not be required to construct a GET URL. Status: fixed for failed offline queue inspection.
- Rehab arm home-action lesson: adding a guide is not enough if the home `daily_action_guide` can still point users to later work while queued/failed offline evidence exists. Surface offline replay/inspection before AI draft review or training start, while preserving active-session and safety-review priority. Status: implemented and tested.
- Rehab arm offline failure lesson: surfacing failed offline items is not enough; users need an evidence-preserving review/close action for genuinely unreplayable items, otherwise `daily_action_guide` can be stuck forever. Status: fixed with `REVIEW_FAILED_OFFLINE_ITEM` and reviewed-state filtering.
- Rehab arm evidence lesson: failed offline item review must require a human note and bounded status values; otherwise the App can clear a blocking state without enough durable context for audit or therapist follow-up. Status: fixed and tested.
- Rehab arm offline review boundary: review closure must not become a bypass for queued replay evidence or already-closed items. Keep the endpoint restricted to `failed` rows and return a clear 409 with current `replay_status` otherwise. Status: tested.
- Rehab arm user-perspective lesson: `daily_action_guide` is useful for commands, but a phone home screen also needs a backend-authored status card (`tone`, headline, blockers, counts, safety note). Otherwise frontend code must infer severity from raw records and can drift from backend priority. Status: implemented as `home_status_guide`.
- Rehab arm home-card lesson: backend-authored home status should reuse the richest available action explanation (`description`, `detail`, or `message`) instead of falling back to generic copy, otherwise first-run users do not understand why setup evidence matters. Status: fixed and tested.
- Home-status product lesson: the first phone card should not force the frontend to search another guide for the obvious next follow-up. When the daily action comes from a source guide with multiple safe actions, expose deduped `secondary_actions` on `/me.home_status_guide` so the user can complete the current blocker from one home surface.
- Home-status source lesson: avoid special-casing one blocker such as offline replay. Add `source.guide` to daily actions and derive secondary home actions from the matching guide `actions`; dedupe by action code and method/endpoint so the first card stays complete without repeating the primary action.
- AI-draft review lesson: `latest_open_ai_draft` is not enough for a usable phone App. Return an explicit `/me.ai_draft_review_guide` with view and accept actions, then let the accepted-plan guide continue through M33 sync, acceptance, preflight, and readiness instead of implying AI acceptance permits exercise.
- Setup guide lesson: ordered `steps` are good for progress display but not enough for a phone CTA surface. Expose executable/read-only `actions` separately on onboarding and start guides so home cards can render remaining setup/readiness operations without guessing from step status.
- Blocker-copy lesson: stable blocker codes are useful for logic, but phone UI should not translate them independently. Return `blocker_details` with title and description from the backend so all shells explain onboarding, reports, AI drafts, and offline evidence consistently.
- Blocker-action lesson: a blocker explanation should also point to the action codes that resolve or inspect it. Keep `related_action_codes` backend-authored so mobile, PWA, and Stitch shells do not drift in how they connect blockers to buttons.
- Home-action grouping lesson: once a home card has primary/secondary actions and blocker details, also return grouped actions. `action_groups.blocker_related` lets the UI render blocker-specific CTAs without duplicating backend matching logic.
- Readiness-blocker lesson: `setup_required` with no blockers is ambiguous after onboarding is complete. If a plan/device exists but cannot start, expose `start_readiness_blocked` so users see that M33 acceptance, preflight, or safety review is the remaining work.
- Primary-blocker product lesson: do not define the phone home blocker as the first raw blocker in storage order. Critical offline evidence can coexist with incomplete onboarding, and the UI must surface the urgent evidence blocker first while still showing the full blocker list.
- Care-summary blocker lesson: if `daily_action_guide` promotes a workflow to the first phone action, `care_summary.blockers` should also explain it. Otherwise the home card can show a button without a matching blocker reason, forcing frontend shells to infer product state again.
- Blocker-resolution lesson: a blocker title and button list still leaves frontend shells guessing when the user asks "how do I clear this?" Return a backend-authored `clear_condition` so UX copy follows the same safety/workflow rules as the API gates.
- Home-progress lesson: progress stages must follow backend blocker priority, not only onboarding completion. A critical offline failure can be the user-visible next step even before setup is complete, so `progress.stage` should track `primary_blocker` when it is not onboarding.
- Progress-rendering lesson: a progress checklist with only `code` and `done` still pushes product copy and blocker/action mapping into each frontend shell. Include backend-authored labels and related codes so mobile/PWA/Stitch screens stay consistent.
- Stage-copy lesson: once the backend owns progress stages, it should also own the user-facing stage title/tone. Otherwise each frontend shell translates stage codes separately and can drift from blocker priority or safety wording.
- Progress-highlight lesson: even with labelled progress rows, frontend shells can drift if each one chooses its own highlighted row. Return `progress.next_item` from the backend and tie it to `primary_blocker` so the highlighted progress row matches the home action and blocker priority.
- Home-progress CTA lesson: highlighted progress rows should carry their current matching actions from the backend. If `next_item` and CTA buttons are derived separately, phone/PWA/Stitch shells can highlight one blocker while showing buttons for another.
- Home-progress blocker lesson: if a highlighted progress row needs a blocker explanation, return the matched blocker details beside it. Otherwise each frontend shell has to join `next_item.related_blocker_codes` against `blocker_details`, and severity/clear-condition rendering can drift.
- Home-progress context lesson: once a phone card needs the highlighted item, its actions, and blocker explanations together, expose a single `next_item_context` object. This avoids repeated frontend joins while preserving the older flat fields for compatibility.
- Home-progress CTA-priority lesson: a next-step context should say which action is primary. If the phone shell chooses its own primary CTA, it can demote urgent backend choices such as failed offline inspection or M33 acceptance.
- Home-progress display lesson: next-step cards need backend-authored copy, not only raw item/blocker data. Add a display object so every shell uses the same title, severity/tone, and clear-condition wording.
- Home-progress percentage lesson: clarify whether a percent is onboarding-only or full workflow completion. For `/me.home_status_guide.progress`, derive percent from the whole checklist so "no active session/no reports pending" can already count as done on first launch.
- Home-progress position lesson: if the backend chooses the highlighted progress row, it should also return the row position and label. Otherwise every phone shell has to rescan the checklist order and can disagree with `next_item`.
- Home-progress row-state lesson: checklist rows should carry backend-authored `done/current/pending` status and tone. Otherwise one shell may highlight the right `next_item` while another styles the same row as merely pending.
- Home-progress row-position lesson: progress rows should carry their own 1-based position labels. Frontend array indexes are easy to lose after filtering or grouping and can drift from backend-selected `next_item_position`.
- Cloud deployment lesson: pushing the rehab-arm App backend branch to GitHub is not enough. Check cloud /api/health and /api/proxy/health deployment metadata; in this run the cloud still reported a4d1c1565de3 until the server fetched app/rehab-arm-mobile-stitch, synced the App backend files, and restarted with AI_COLLAB_BUILD_SHA=17d7e78b2b10.
- Cloud dirty-worktree lesson: the server repo can contain unrelated local changes on another branch. Do not blindly git checkout the whole feature branch there; fetch the exact ref, sync only the required backend files, run the App backend tests, then restart. A partial sync first failed with 404s because app_router.py, app_schemas.py, main.py, and the rehab_arm_app DB model registration were missing from the older cloud branch.
- Profile onboarding lesson: PATCH endpoints must accept the exact payload shape advertised by onboarding_guide payload_hint. Requiring a hidden name field made the first phone setup action fail with validation before the user could save affected_side, rehab_stage, constraints, and pain baseline. Status: fixed with partial profile updates and default name creation.
- Profile completion lesson: a non-null profile row is not enough for rehab onboarding. The backend must check the specific safety-context fields it later uses, or users can advance without affected side, rehab stage, or pain baseline. Status: fixed with missing_fields on PROFILE_REQUIRED.
- APK release lesson: do not rebuild and publish the Android package as user-ready while mobile-bridge.js still defaults to no API base and local preview data. Backend-only cloud fixes take effect for connected clients, but the installable APK needs frontend/Stitch login/API wiring before a meaningful new download build.
- Mobile config lesson: the installable APK should not hard-code cloud URLs or silently fall back to preview data. Expose a public, secret-free config endpoint with API base, login endpoint, bootstrap endpoint, APK status, and safety boundary so frontend shells can connect to the real backend without guessing.
- Mobile boot lesson: a public config endpoint should include the exact token response path and Authorization header pattern. Otherwise an APK can know the API base but still call protected /me endpoints with cookies or no bearer token and fall back to preview data.
- Mobile CORS lesson: after publishing a boot contract, the next failure can be browser/WebView preflight rather than API logic. Allow Capacitor origins explicitly (configurable via REHAB_ARM_APP_CORS_ORIGINS) and test OPTIONS from https://localhost before claiming the APK can call cloud APIs.
- Workflow-action lesson: phone buttons should execute backend-authored `action_code` values through `/api/rehab-arm/app/v1/me/workflow/actions`, not arbitrary frontend-composed URLs. The backend must first confirm the action appears in the current `/me/workflow.action_queue`, then apply a whitelist; M33 decisions, direct motor commands, CAN frames, M33 overrides, emergency-stop release, and App-granted motion permission stay explicitly forbidden until real hardware protocols and authority boundaries are supplied.
- Care-timeline rendering lesson: `/me.care_timeline` should carry backend-authored display copy, tone, primary action, and related entity ids for each item. Otherwise phone/PWA/Stitch shells will each translate statuses differently and may offer the wrong report, session, AI draft, or offline-evidence action.
- Daily-care-plan product lesson: once `/me.home_status_guide` and `/me.care_timeline` exist, the phone still needs one render-ready daily plan. Expose `/me.daily_care_plan` as the canonical home checklist/action/blocker/timeline bundle so Stitch/mobile shells do not recompute the user's next task differently or fall back to demo cards.
- Workflow-onboarding lesson: setup actions should use the same `/me/workflow/actions` contract as training/report actions so phone shells do not special-case first launch. Keep them state-gated by the current action queue, and do not let the workflow shortcut bypass project authorization for `platform_project_id` device binding.
- Workflow-sync lesson: exposing `SYNC_PLAN_TO_M33` through `/me/workflow/actions` is safe only because it reuses the existing plan-sync record path and stops at `pending`. Keep `M33_ACCEPTANCE_REQUIRED` and `RECORD_M33_DECISION` out of App-executable actions until real hardware authority and packet protocols are supplied.
- Workflow-preflight lesson: `PREFLIGHT_CHECK_REQUIRED` can be App-executable only after the current plan version has real `m33_accepted` evidence. Execute it by reusing the normal preflight service so pain thresholds, checklist completeness, sync id, and plan-version gates stay centralized.
