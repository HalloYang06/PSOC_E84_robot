# 双核固件与应用同步记录（2026-07-19）

## 同步目标

- 硬件主仓库：`HalloYang06/PSOC_E84_robot` 的 `main` 分支。
- M33 固件目录：`firmware/m33`。
- M55 固件目录：`firmware/m55`。
- App 与 NanoPi：保留在 `wenjunyong666/ai-` 的 `app/rehab-arm-mobile-stitch` 分支，不复制进固件目录。

## 来源版本

- M33：`fb4cd784b`（包含三分类 EMG 上报切换说明）。
- M55：`9c987180b`（包含三分类模型、单仓库唤醒后端路径兼容和构建门禁修复）。
- App/NanoPi：`2e7db43`（三分类意图标签对齐）。

## 快照边界

- 固件快照不包含各独立工作区中未提交的文件、构建产物和运行日志。
- M33 原工作区的 `cloud` 内容不进入 `firmware/m33`；App、NanoPi 和平台代码由 App 分支维护。
- 单仓库 M55 会沿父目录查找本机 BSP 中的 Edge Impulse 资源；资源缺失时保持唤醒后端未链接，不再误启用缺少闭源依赖的 DeepCraft 后端。

## 验证基线

- M33 与 M55 均需在各自的 `firmware` 子目录完成 SCons 链接。
- M55 ELF 应包含 `infineon_official_xiaozhi_edge_impulse`、`shoulder_flex`、`elbow_curl` 和三输出推理日志。
- App/NanoPi 分支推送前需通过 NanoPi 三分类上报脚本测试。
