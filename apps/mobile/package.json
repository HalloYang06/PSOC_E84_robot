{
  "name": "rehab-arm-android",
  "private": true,
  "version": "0.1.0",
  "description": "Android wrapper for the 灵动康复 ArmControl mobile PWA.",
  "scripts": {
    "sync:web": "node ./scripts/sync-web-assets.mjs",
    "cap:sync": "npm run sync:web && cap sync android",
    "build:debug": "npm run cap:sync && cd android && gradlew.bat assembleDebug",
    "doctor": "node ./scripts/doctor.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/core": "^8.4.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.1"
  }
}
