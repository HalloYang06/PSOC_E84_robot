BTSTACK core template directory.

First-wave import list fixed for this project:
- hci.c
- l2cap.c
- rfcomm.c
- sdp_client.c
- sdp_server.c

Expected next action:
- import these upstream BTSTACK core files into this directory or map them into the build from an upstream vendor directory
- keep btstack_core_entry.c as the local integration entry until real sources replace it
