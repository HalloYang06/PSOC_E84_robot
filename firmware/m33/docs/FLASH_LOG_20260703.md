# M33 / M55 烧录记录 2026-07-03

## 目标

把当前本地 M33 和 M55 工程重新构建并烧录到 PSoC Edge E84 板子上。

本次使用的工程：

```text
M33: F:\RT-ThreadStudio\workspace\Edgi_Talk_M33_Blink_LED
M55: F:\RT-ThreadStudio\workspace\Edgi_Talk_M55_Blink_LED
```

## 设备识别

Windows 当前识别到的板载调试器：

```text
KitProg3 CMSIS-DAP
USB parent serial: 1C161868022E2400
UART: COM20
```

注意：旧文档里出现过 `17040F11022F2400`，这次实际连接的板子不是这个 serial。烧录时必须以当前 Windows 枚举结果为准。

## 构建结果

M33 构建命令：

```powershell
cd F:\RT-ThreadStudio\workspace\Edgi_Talk_M33_Blink_LED
$env:RTT_EXEC_PATH='F:\RT-ThreadStudio\platform\env_released\env-new\tools\gnu_gcc\arm_gcc\mingw\bin'
F:\RT-ThreadStudio\platform\env_released\env-new\.venv\Scripts\scons.exe -j4
```

关键结果：

```text
Edge Protect relocated 0x08340400... to 0x60340400...
Saved file to 'build/rtthread.hex'
scons: done building targets.
```

M55 构建命令：

```powershell
cd F:\RT-ThreadStudio\workspace\Edgi_Talk_M55_Blink_LED
$env:RTT_EXEC_PATH='F:\RT-ThreadStudio\platform\env_released\env-new\tools\gnu_gcc\arm_gcc\mingw\bin'
F:\RT-ThreadStudio\platform\env_released\env-new\.venv\Scripts\scons.exe -j4
```

关键结果：

```text
scons: `.' is up to date.
scons: done building targets.
```

## 烧录结果

OpenOCD 路径：

```text
F:\RT-ThreadStudio\repo\Extract\Debugger_Support_Packages\Infineon\OpenOCD-Infineon\2.0.0\bin\openocd.exe
```

M33 烧录文件：

```text
F:\RT-ThreadStudio\workspace\Edgi_Talk_M33_Blink_LED\build\rtthread.hex
```

M33 OpenOCD 关键结果：

```text
wrote 585728 bytes
verified 583188 bytes
```

M55 烧录文件：

```text
F:\RT-ThreadStudio\workspace\Edgi_Talk_M55_Blink_LED\rtthread.hex
F:\RT-ThreadStudio\workspace\wifi_resources\whd_resources_all.bin
```

M55 OpenOCD 关键结果：

```text
rtthread.hex wrote 1789952 bytes
rtthread.hex verified 1785440 bytes
whd_resources_all.bin wrote 466944 bytes
whd_resources_all.bin verified 465509 bytes
```

OpenOCD 在 reset/exit 阶段仍打印过：

```text
kitprog3: failed to acquire the device
```

但该信息出现在写入和 verify 之后，本次不作为烧录失败处理。

## 串口 smoke

串口：

```text
COM20 / 115200
```

发送命令：

```text
m55qa_status
```

关键回包：

```text
[m55qa] ipc_ready=1 tx_pending=0 rx_pending=0
[m55qa] voice_status ... wake_on=1 wake_ready=1 ...
[m55qa] netdev name=(none) ... wlan=0 ready=0 ... xz_token=0 ...
```

结论：

```text
M33 shell 可用。
M33 -> M55 IPC 可用。
M55 运行状态可通过 M33 查询。
WiFi / XiaoZhi token 当前未连接或未配置，不影响本次烧录结论。
```

## 下一步验证

如果要验证肌电小模型，应继续做：

```text
1. 确认 M55 侧 intent TFLM runtime 已启动。
2. 跑固定 golden sample。
3. 打开 M33 EMG stream。
4. 看 M55 推理结果是否返回 elbow_flex / elbow_extend / rest / shoulder_flex。
5. 再验证小智语音能否切换 M33 控制模式。
```
